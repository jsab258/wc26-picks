# THE ANIMATION CLIP DEBT — measured, and counting down only.
#
# STATUS — LIVE, verified 2026-08-21.
#
# `tools/clip-motion.py` reads every clip FBX we ship and reports the two
# faults a file can prove on its own: two slots holding the SAME BYTES, and a
# clip whose hips neither move nor turn across its whole length. `verify.py`
# compares the count against the number below and fails when it rises, so a
# re-pick cannot quietly introduce a new one.
#
# THE SAME SHAPE AS THE REACH LEDGER, and for the same reason: the debt is
# real, it cannot be cleared today, and a guard that demands zero on day one
# is a guard nobody can commit past. This one counts down.
#
# WHY IT IS NOT ZERO. Both faults are in the HARVEST, which lives on Jafar's
# Windows machine and is the one input this container cannot reach. The fix
# is a re-pick, which needs no token and takes seconds — the picker now skips
# a candidate whose bytes are already in another slot, and the WANTS list has
# alternate names for the clips involved.
#
# LOWER THIS NUMBER WHEN THE FINDINGS DROP. Never raise it to make red go
# away; that is the move rule 2 forbids.

clipFindings=2

# CLOSED 2026-08-18, ON THE FIRST RUN OF THE GUARD THAT FOUND IT:
#
# 1. DUPLICATE: `shoved` (Shove Reaction) == `talk` (Talking), byte for byte.
#    `_picks.json` recorded the picker matching two DIFFERENT harvest names
#    exactly, so the collision was made by the bulk harvester, not here — and
#    the file carries no distinguishing take name (every Mixamo FBX calls its
#    take "mixamo.com"), so which slot was wrong could not be told from the
#    bytes.
#    CLOSED by the content check doing exactly what it was written for, on
#    Jafar's machine, in the first re-pick after it shipped: it skipped the
#    duplicate for `talk` and took `Talking_4f5d21e1` instead — a different
#    character's export of the same motion. The audit now reads "no two clips
#    share content" over 53 clips.
#    NOT CLOSED BY THE PART I PREDICTED. The commit said the fix was the
#    picker preferring `Shoved`; that pattern was written `^shoved$` and
#    matched nothing, so `shoved` kept Shove Reaction and the CONTENT check
#    carried the whole repair. The specific fix was wrong and the general one
#    worked, which is the argument for having written the general one.
#
# THE SECOND RE-PICK, 21 Aug: 62 copied, 5 substituted, 5 missing, 5 SET ASIDE.
# NOTHING IN THE GAME NOW PLAYS A CLIP THE SCREEN REFUSES — audited slot by
# slot, 62 filled and passing, 5 empty, 0 refused-but-shipped. The five empty
# ones fall back to the locomotion tree, so those people stand normally rather
# than playing somebody else's motion.
#
# TWO THINGS CAME OUT OF READING IT, AND BOTH WERE INVISIBLE TO EVERY CHECK.
#
# A. TEN OF MY OWN PATTERNS COULD NEVER MATCH ANYTHING — a doubled backslash,
#    `r"^sitting\\b"`, which asks for a literal backslash in a clip name. All
#    ten were made in one edit: replacing the `$` anchors that had cost
#    fourteen slots, with the paragraph explaining why `$` cannot match sitting
#    directly above. One impossible form fixed by writing another.
#
#    `block_hold` was down to ONE live pattern out of five and `sit_drink` to
#    two of three — so the widening written to fill those exact holes was inert
#    for the exact slots it was for, and the run still reported `none of 5
#    patterns matched`, which reads as a harvest with nothing to offer.
#
#    THE GUARD SAID IT CAUGHT THIS AND THE CLAIM WAS FALSE THE DAY IT WAS
#    WRITTEN: "the `$` rule catches one way of writing an impossible pattern;
#    this catches all of them, including the ones nobody has thought of". It
#    asks whether a SLOT matched, and a slot is satisfied by any one of its
#    patterns, so a dead pattern beside a live one is structurally invisible to
#    it. Fixed both ways — a mechanical doubled-backslash check next to the `$`
#    one, and a per-PATTERN report with its denominator, which reads 137 of 145
#    live. The 8 dead ones are honest alternates this harvest lacks
#    (`\bsurrender\b` for hands_up); they are printed, not failed, because a
#    pattern matching nothing is only a bug when it is impossible.
#
# B. A SECOND HARVESTER DUPLICATE, AND IT LANDED ON A COMBAT SLOT.
#    `Unarmed Equip Underarm_4f5d21e1` and `Standing Arguing_4f5d21e1` are
#    byte-identical. The content check found it and skipped `argue` onto
#    `Angry Gesture` — correctly, but only because tier A runs first and
#    `draw_reach` had already claimed the bytes. Reported and resolved in
#    favour of whoever went first is not resolved correctly.
#
#    WHICH NAME IS RIGHT CANNOT BE READ FROM THE BYTES. The DURATION can:
#    20.80s, third longest of 62, against a median of 2.00s and a p90 of 7.57s.
#    The only two longer clips are a sitting conversation and a phone call,
#    both loops meant to run. Every other one-shot in the set — strike, shove,
#    take_hit, draw_gun — sits near the median. A man reaching under his coat
#    does not take twenty seconds; a man arguing does.
#
#    `draw_reach` now asks for `Grabbing Pistol` first, a third name for the
#    motion actually wanted, which sidesteps the collision with no re-download.
#    NOT COUNTED IN `clipFindings`: only one of the two names ships, so the
#    duplicate audit correctly reads `duplicates=0`. It is a naming collision
#    upstream, not two slots sharing bytes here.
#
# CLOSED 2026-08-21 BY THE RE-PICK, AND NOT THE WAY THIS FILE PREDICTED:
#
# 1. FROZEN ROOT: `walk_start` (Start Walking). The paragraph below said all
#    three needed a re-download because "the catalogue holds no alternate name
#    for any of the three". That was true of the NAME and the fix did not come
#    from a name — the travel screen refused `Start Walking` (0.00m in a GOES
#    slot) and `pick` fell through to the next candidate under the same
#    pattern. `walk_start` now reads 97.96cm of hip motion over 5.0s, travels
#    0.94m. Audited, not assumed: `clipFindings=2 duplicates=0 frozen=2`.
#
#    AND THE CLIP IT FELL THROUGH TO IS `Start Walking Backwards`.
#    Which is the screen working exactly as designed and producing a clip
#    nobody wants: a man setting off is going to set off backwards. THE TRAVEL
#    AXIS IS UNSIGNED — `travel` is `hypot(endX-startX, endZ-startZ)`, a
#    distance — so forwards and backwards are the same number to it and always
#    will be. It is not a bound to tighten; it is a question the metric cannot
#    be asked. Direction needs the sign of the displacement along the body's
#    own facing, which the reader already has the data for.
#
#    NOT FILED AS A FINDING, because `clip-motion` reports only what the FILES
#    prove and this file proves nothing wrong — it is a correct Start Walking
#    Backwards. Filed here so the next person does not read `frozen=2` as
#    "walk_start is fine".
#
#    SECOND OBSERVATION, UNRESOLVED: its hips sit at 145/148/150cm where every
#    other upright clip in the set reads 88-104. It is a `4f5d21e1` export and
#    most of the set is `2dee24f8`, so this is a differently-scaled rig rather
#    than a body standing on something. Humanoid retargeting normalises scale,
#    so this is probably nothing — but "probably" is doing the work, nothing
#    has looked, and the posture band cannot see it: `upright` is anything at
#    or above 39cm, so 148 passes as easily as 95.
#
# WHAT THE REMAINING TWO ARE:
#
# 1-2. FROZEN ROOT: `lean` (Leaning), `block_end` (Standing Block End). The
#    hips are the root of a Mixamo rig and carry the body's whole world
#    motion, so a clip whose hips move less than a centimetre and turn less
#    than two degrees is animated from the waist up. For a Standing Block End
#    and for a Leaning that is arguable — both are motions a body performs
#    without going anywhere, which is why these two are the ones left after
#    the walk did not survive its own name.
#    The catalogue holds no alternate name for either, so these need a
#    re-download rather than a re-pick, and neither is wired to anything today.
#
# THE FIRST CONTACT SHEET WAS THE INSTRUMENT'S FAULT, NOT THE GAME'S — 18 Aug.
# `clips.jpg` on 340e958 drew all 67 clips and every one of them showed the same
# broken figure: one arm clawed up beside the head, the other a detached blob,
# and the shoes lying on the ground away from the feet. Read as a finding it
# would have condemned the whole harvest, the body pool and the retarget.
#
# The same build's `review_day1_noon.jpg` shows a man in a blue suit walking
# across Hook Street with his arms swinging and his feet planted, and he looks
# good. Two pictures from ONE build disagreeing is the tell, and the ruler was
# the one at fault (rule 3).
#
# The cause was mine and it was one line. The sheet disabled every MonoBehaviour
# on the body so the picture would be "of the CLIP" rather than of the clip plus
# the procedural layer. But `CharacterRig.Awake` calls `Bind()` and runs whether
# the component is enabled or not — so disabling it kept the bind and removed
# the LateUpdate that finishes it. Disabling half a system is not the same as
# not having it. The premise was wrong as well: what a player sees IS the clip
# plus the procedural layer, so a picture of the clip alone is a picture of
# something nobody ever looks at.
#
# Fixed by leaving every component on and giving the body a floor to stand on.
# NO CLIP IS ACCUSED ON THE STRENGTH OF THAT SHEET, and the count above is
# unchanged by it — it reports only what the FILES prove.

# AND THE FIX'S OWN BUILD NEVER RAN, FOR A REASON THAT WAS ALSO MINE — 18 Aug.
# The run on bf0aab4 died at `actions/checkout` (two minutes, everything after
# it skipped), which is not my code. What IS mine is that it produced no
# verdict at all: the step that always writes one exited 1 first, on
# `cp: cannot stat 'sim-run/sim-out/clips.jpg'`.
#
# The copy block I had added read `sheet=(sim-run/sim-out/clips.jpg)` and
# tested the array length — copied from the `shots=(...*.jpg)` line above it
# without noticing that `nullglob` only empties a pattern containing a
# WILDCARD. A literal path has none, so the array always held one element, the
# `cp` always ran, and the first build where the sim rendered nothing died
# there under `set -e`.
#
# That is rule 12 wearing my own patch: the run that most needed a verdict was
# the one guaranteed not to get one, and the reason the checkout failed had to
# be read out of the job API instead. `[ -f ]` now, with `|| true` on the
# copies, and all three cases — no sheet, sheet only, sheet plus ledger — were
# run against the exact text in the workflow before committing. The old form
# was run too, and fails exactly as CI did.
#
# THE LESSON THAT GENERALISES: an artefact this pipeline ADDS must never be
# able to cost the pipeline its feedback channel. Guard every new copy with a
# test of the file, and run the empty case.

# WHAT THIS FILE DELIBERATELY DOES NOT CLAIM. It does not say which animation
# a mislabelled clip really is. Hip HEIGHT is not comparable between clips (a
# stand-up legitimately starts at 8cm) and Euler ranges inflate on wrap (the
# walk reads 131 degrees of hip roll), and two readings that looked like
# findings died on exactly those two facts. The file says what it can prove.
# The rest is a question for a frame.

================================================================
2026-08-18 — THE CONTACT SHEET, READ WITH AN INSTRUMENT
================================================================

The sheet from b153d64 renders 67 of 67 slots and the POSES ARE GOOD: real
retargeting, feet on the floor, articulated hands, no T-poses, no clawed arms,
no detached shoes. The pose fault reported against the first sheet is closed.

WHAT IS WRONG IS THE EXPOSURE, AND IT IS NOT THE CLIPS.

  sheetLegible=40 sheetDark=25 sheetSmall=1 sheetEmpty=1 sheetClips=67
  sheetLuma=14..108

Plotted against RENDER ORDER rather than against slot name, the body luma
traces two clean day/night cycles: 61 at the first clip, down to 14 in the
trough, up to 77 at the peak, down again, rising at the last. The game's
lighting pass writes RenderSettings.ambientLight from the clock every frame,
each tile costs one frame, and 201 frames is long enough for the in-game clock
to run through two dawns. So a clip is dark because of WHEN in the sheet it was
drawn, not because of anything about the clip.

  The fix is already written and has never run: ClipSheet re-asserts the sheet
  ambient AFTER the per-tile `yield return null` and immediately before
  cam.Render(), which is the only ordering that survives a lighting pass. It
  was committed as ab954a2, whose build died in actions/checkout.

AND THEN "ARE WE USING WRONG ANIMATIONS": THREE SLOTS LOOK WRONG IN THE
PICTURE, AND THE PICTURE IS ALL THE EVIDENCE THERE IS. It is enough to open
the question and not enough to close it.

  lie_still  renders as a figure MID-STRIDE across all three phases — one leg
             raised, arms bent at the elbow.
  jog        renders as a body flat on its back, arms out, all three phases.
  collapse   ships as "Dying" (4.00s) and renders as a STANDING figure with an
             arm out at 15%, 45% and 78%.

All three sit on BRIGHT tiles, so none of these is a reading made on a
silhouette, and the sheet's own mapping was checked before believing any of it:
`yell`, the 67th and last clip, lands alone in the three-tile bottom row, and
`phone_box` renders as a man with a hand at his head. Rows and columns are
`i / 6` and `i % 6` and the ledger is written from the same pair. The
controller binds by the filename's slot prefix — the same discriminator
clip-motion reads — so the sheet and the file reader agree about WHICH FILE
backs each slot.

I THEN TRIED TO CORROBORATE IT FROM THE FBX AND THE CORROBORATION DOES NOT
HOLD, which is the part worth writing down. `lie_still` measures 2.18m of
travel in 1.67s and I published that as "a walking speed, so it is not a
lying-down animation". Reading the whole column kills it:

  Walking                    travel 0.00      Female Walk   travel 1.22
  Standing React Large Gut   travel 2.29      Lying Down    travel 2.18
  Jog Forward                travel 0.00      Running       travel 1.67

A standing reaction does not travel 2.29m in 0.73s (3.1 m/s), and one walk
reading 0.00 beside another reading 1.22 is not a property of walks. So the
column does not track what the animation IS, and cannot be used to argue about
what a clip contains. `measure()` takes the HIPS' local translation curves,
which is right only while the hips are unparented and carry the world motion;
that assumption is the thing to check.

  OPEN: clip-motion's travel/path columns are not trustworthy for this
  question. Nothing else in the project reads them, and the duplicate and
  frozen-root findings — which are what `clipFindings` counts — do not depend
  on them.

So the honest state is: three slots are SUSPECTED of playing the wrong motion,
on picture evidence alone, and the way to settle it is a legible sheet where
all 67 can be read side by side rather than 42 of them.

The sheet's own mapping was checked before believing any of this, because a
row-indexing slip would produce exactly this symptom: `yell`, the 67th and last
clip, lands alone in the three-tile bottom row, and `phone_box` renders as a man
with a hand at his head. Rows and columns are `i / 6` and `i % 6` and the ledger
is written from the same pair. The controller binds by the filename's slot
prefix — the same discriminator clip-motion reads — so the sheet and the file
reader agree about WHICH FILE backs each slot. The disagreement is between the
file's NAME and its CONTENTS, which is upstream of both.

RETRACTED, from an hour earlier in this same session, and both were mine:

  "get_up draws nothing"    — it draws a body lying on the floor. `sheet-read`
                              was excluding the floor band by HEIGHT to keep
                              the lit plane out of the luma, and a prone body
                              lives entirely inside the band being discarded.
                              Area is measured over the whole tile now and luma
                              above the floor line, because they are two
                              questions.
  "get_up and jog travel"   — read off the wrong COLUMN. clip-motion prints
                              `secs movecm turn deg travel m path m`; I took
                              movecm as travel and turn as path, and published
                              "Stand Up travels 2.78m along a 150.7m path". It
                              travels 0.01m along 0.20m. The clips that DO
                              travel are Talking (2.77m) and Standing Arguing
                              (3.75m).

The re-centring code written for that wrong diagnosis is kept, downgraded from
a fix to a measurement: `applyRootMotion` is false so `sheetSlid` should read
zero, and nothing has ever confirmed that.

AND ONE SUSPICION THAT IS NOT YET EVIDENCE, recorded so it is not re-derived:
`walk_stop` renders as a deep crouch and `sit` as a standing figure at phase
0.15. Both are plausible as legitimate phases of their motion, and both sit on
tiles a third of whose neighbours cannot be seen. Judge on a legible sheet.

ON THE INSTRUMENT ITSELF (tools/sheet-read.py), because it was wrong twice
before it was right, in opposite directions, inside one afternoon:

  DARK_LUMA=42  passed 67 of 67 on a sheet with visible silhouettes. The floor
                plane across the bottom quarter of every tile is brighter than
                the background, counted as body, and dragged a silhouette's
                mean from about 6 up to 46.
  DARK_LUMA=60  then failed 54 of 67 including clips confirmed fine by eye.
  DARK_LUMA=27  is measured: the floor is excluded by detecting the step in the
                median row luma across all 201 tiles (90.0 at y=164, 118.0 at
                y=166), and the bound sits where the tiles stop being readable
                — walk_old at 25 is still a silhouette, sit_talk at 28 shows
                face, tie and shoes.

Both wrong values were invented rather than measured, which is rule 2, twice,
after quoting rule 2 in the commit that introduced the tool.


================================================================
2026-08-18 LATE — THE ANIMATIONS, ANSWERED. NINE ARE WRONG.
================================================================

The exposure fix landed (sheetDark 25 -> 0, sheetLuma 14..118 -> 33..190), so
all 67 clips could be read at once for the first time. Every one was cropped
individually with its slot name drawn on it, rather than located by arithmetic
in a 201-tile grid, because position arithmetic is how the last three wrong
readings happened.

FIVE CLIPS DO NOT CONTAIN WHAT THEIR NAME SAYS, ON TWO INSTRUMENTS, AND FIVE
MORE ARE SUSPECTED ON THE PICTURE ALONE.

The first version of this section said NINE, "confirmed by two independent
instruments", and that was wrong — written before printing the hip height of
all 67 rather than of the ones I was already suspicious of. Rule 2, again, and
this time caught by the series rather than by a build.

  CONFIRMED — the render and the FBX agree, and the height is unambiguous:

  slot        file name                   hips lo/md/hi   what it actually is
  jog         Jog Forward                   7 /  7 /  7   a body flat on the floor
  lie_still   Lying Down                   85 / 96 /121   an upright stride
  collapse    Dying                       103 /103 /103   never leaves standing
  sit         Sitting Idle                 92 / 96 /101   standing, arm raised
  sit_talk    Sitting Talking              -  / 94 / -    standing

  SUSPECTED — the render looks wrong and the height CANNOT corroborate it,
  because correct clips share the same band:

  carry_bag   Walking With Shopping Bag    60 / 60 / 62   crouched, travel 0.00
  idle_bored  Bored                        63 / 64 / 67   crouched
  head_no     Shaking Head No              48 / 69 /103   seated; 11.5s long
  walk_stop   Stop Walking                 43 / 76 / 81   drops into a crouch
  thinking    Thinking                     70 / 72 / 74   a wide-armed stride

WHY THE SECOND GROUP CANNOT BE CLOSED ON HEIGHT. Printing the median for ALL
67, sorted, shows one clean gap and one crowded band:

  7  jog · 9 get_up · 14 knockdown · 18 sit_drink   <- on the floor
     ...a gap with nothing in it from 18 to 60...
  60 carry_bag · 64 idle_bored · 68 guard_exit · 69 head_no · 72 thinking
  73 turn_right · 74 block_broken · 74 RUN · 76 walk_stop
  80..103  everything else, upright

**`run` reads 74 and is correct.** So is `block_broken` at 74. Any bound that
rejects `walk_stop` at 76 rejects those, which is rule 5b's ratchet exactly.
The floor gap at 18..60 is real and usable; the crouch band is not separable by
height and needs a different measurement or a person.

  jog and lie_still are EACH OTHER. That is not five independent bad
  downloads; an exact swap is a slot/content mix-up.

Hip height is in centimetres and a standing adult reads about 95. CLAUDE.md
warns that hip HEIGHT is not comparable between clips, and that warning is
right about the question it was written for — you cannot tell WHICH animation a
clip is from it. It answers only "upright or on the floor", and only across the
gap that the distribution actually shows.

THE CONTROLS, which matter as much as the findings — a checker that flags
everything has found nothing:

  walk        Walking          93 / 95 / 95   upright, correct
  idle        Breathing Idle   94 /100 /105   upright, correct
  get_up      Stand Up          8 /  9 / 11   starts prone, correct
  knockdown   Knocked Out       6 / 14 /104   standing to floor, correct
  block_start Standing Block    -              hands up at the face, correct

WHERE THE FAULT IS NOT. The picker chose correctly: every file on disk carries
the right Mixamo title, and `pick_animations.py --dryrun` resolves 65 exact and
2 substitutes with 0 missing. The controller binds by the filename's slot
prefix. The sheet's grid is sound (`yell`, the 67th clip, lands alone in the
three-tile bottom row). So the disagreement is between a harvest file's NAME
and its CONTENTS, upstream of everything in this repository.

  An earlier note in this file said the picker's patterns were the next thing
  to check. They were checked and they are fine. The one real pattern smell
  left is `walk_stop`'s second, unanchored `\bstop walking\b`, which also
  matches "Crouch Stop Walking" — and the clip renders as a crouch. That is
  suggestive and it is NOT the explanation, because the file on disk is named
  "Stop Walking", not "Crouch Stop Walking".

WHAT FIXES IT. A re-pick, not a re-download: the full harvest (~2,500
animations per character) is already on Jafar's machine and REPICK.bat runs
against it in seconds with no token and no downloads. What the picker needs is
to VERIFY the candidate rather than trust its name — the hip-height reading
above is exactly the discriminator, and it would have rejected all nine.

  A CAUTION FOR THAT WORK, and it is rule 5b twice over: the check must ACCEPT
  `get_up` at 8cm and `knockdown` at 6..104, and it must not try to police the
  crouch band, where `run` at 74 and `walk_stop` at 76 are one correct and one
  wrong. It can only assert the thing the distribution supports — a slot that
  must be ON THE FLOOR against one that must be UPRIGHT, across the empty gap
  from 18 to 60.

----------------------------------------------------------------
AND THE RE-CENTRE MEASUREMENT CAME BACK NON-ZERO, WHICH IS A FINDING
----------------------------------------------------------------

`sheetSlid=73 sheetSlidWorst=0.20` against `sheetTiles=67` (201 phase tiles).

It was written expecting ZERO. `applyRootMotion` is false, so in principle the
transform never accumulates and the hips stay over the origin — and nothing had
ever checked that "in principle". Seventy-three of 201 tiles needed moving, by
up to 20cm.

20cm does not push a body out of a frame 3.9m away, so this was never the cause
of anything visible, and the earlier claim that it was is retracted elsewhere in
this file. It is worth keeping for exactly the reason it was worth printing: a
number that was assumed to be zero and is not.

The exposure is also not fully pinned even after the fix — `sheetLuma=33..190`,
a 157-level spread, against 14..118 before. Nothing is unreadable any more
(`sheetDark=0`), which is what the fix was for, so the residue is not blocking.
The floor plane now reads 254, i.e. blown out; that is the ambient re-assert
landing on a surface that was already bright, and it costs nothing because the
floor carries no information.

================================================================
2026-08-19 — IT IS NOT TEN CLIPS. IT IS ABOUT A THIRD OF THEM.
================================================================

The "ten wrong animations" finding was right and far too small, and the thing
that found the rest is the column I had written off the day before.

`clip-motion`'s travel figure read `Walking` 0.00m and `Standing Arguing` 3.75m,
and I dismissed it: a standing argument cannot travel 3.75 metres, so the
instrument must be wrong. That inference has a hole in it. It CAN travel that
far — if the file called `argue__Standing_Arguing` does not contain a standing
argument. The column was telling the truth about files that lie, and reading it
as a broken instrument cost most of a day.

WHAT THE RENDERS SAY, on the legible sheet, for the ones travel flagged:

  idle          renders MID-STRIDE, feet alternating          travels 1.74m
  talk          renders as locomotion with gestures           travels 2.77m
  walk          renders as a STATIONARY guard pose, hands up  travels 0.00m
  pockets       renders as a man walking                      travels 2.42m
  laugh         renders as a man walking                      travels 1.63m
  guard         renders as a throw — arm out, leg up, twisted travels 3.12m
  argue         folds double, head at knee height, mid-clip   travels 3.75m

Every one agrees with its travel figure. Two independent instruments, and the
one I discarded is the one that scales — it needs no picture and no build.

  `walk` is the sharpest of these. The slot the whole street is named after
  does not contain a walk.

THE SCREEN, RUN OVER ALL 67: twenty-one rejected, forty-six accepted. Nine fail
because they should go somewhere and do not, or should stand still and walk off;
the rest fail on hip height. So roughly a third of the harvest does not contain
what its filename says.

  This is not a picking fault and it was checked before being blamed: the
  picker resolves 65 exact / 2 substitute / 0 missing, every file carries a
  real Mixamo title, and the controller binds by that title's slot prefix.
  The names are right and the CONTENTS are not, which is upstream of this
  repository and of the picker.

THE PICKER NOW SCREENS ON BOTH AXES. Hip height answers "upright or on the
floor"; travel answers "does this move". Bounds come from the measured gap
rather than a guess — everything that renders as locomotion travels 1.0m or
more, everything correct and stationary sits at 0.00-0.06m, so 0.15 and 0.50
leave that gap open on both sides.

  AND `walk` STOPPED BEING AN ACCEPTING CASE IN THE SELFTEST, which is worth
  writing down: it was one, and freezing a known-bad clip into a fixture as
  proof of correctness is rule 5b failing in the direction nobody checks. The
  accepting cases are `run`, `walk_f`, `knockdown` and `get_up` now, and `run`
  is still the load-bearing one — it reads 74cm, inside the crouch band, and
  any hip bound tight enough to catch `walk_stop` at 76 would reject it.

WHAT THIS MEANS FOR THE RE-PICK. It will now REFUSE a candidate whose contents
contradict its name and try the next one. Where the catalogue holds an
alternative that is a fix; where it holds only the one name — `thinking` matches
"Thinking" and nothing else — the slot will report MISSING instead of shipping
the wrong motion. Holes are the correct outcome and they are information: a
slot that cannot be filled from a 2,846-name harvest says the harvest needs
redoing rather than re-picking.

STILL NOT CAUGHT, and stated so the gap is not mistaken for a clean result:
`sit` renders standing at 96cm and passes both axes, because sitting has no
band anybody can set from evidence — the three sitting clips read 18, 94 and 96
and not one of them is at chair height, so there is no correct example to
calibrate against.

2026-08-26 — RAISED 2 -> 3, AND IT COMES BACK DOWN, NAMED SO IT CANNOT ROT.
The third finding is `walk_start` and `walk_start_f` holding THE SAME FILE
(`Female Start Walking`). Cause: `walk_start`'s second pattern is unanchored
(`\bstart walking\b`), no male start clip was in the harvest, so it matched
the female one and both slots took it. `clip-motion` caught it the moment the
drop landed and named it exactly — the instrument working, not failing.
THE FIX IS ALREADY IN THE PICKER: `gender_ok` refuses a female-named clip for
a general slot when an `_f` twin exists (walk, walk_start, walk_stop), tested
both ways. It cannot take effect here because a re-pick needs the full Mixamo
harvest, which lives on Jafar's machine and is not in this repo.
SO THIS NUMBER IS DEBT WITH A NAMED DISCHARGE, not an accepted finding: after
the next re-pick the count returns to 2 and the ratchet itself will go RED
until this line is put back — which is the mechanism doing its job in the
other direction. Queue: `repick-after-gender-guard`.

DISCHARGED THE SAME MORNING, 26 Aug. The re-pick ran with the guard and the
duplicate is gone: `walk_start_f` holds `Female Start Walking` and
`walk_start` no longer shares it. `clipFindings` measured back at 2,
duplicates 0. Lowered 3 -> 2, and the ratchet DID go red in the other
direction first, exactly as written above — which is the only reason anybody
had to come back and lower it.

AND THE NEXT THING THE FALLBACK GRABBED IS A DECISION, NOT A BUG. With the
female clip refused, `walk_start`'s unanchored pattern reached
`Catwalk Walk Start Turn 180 Left` — a runway turn, so every man would begin
walking with a sashay. **`pick_animations.py` PREDICTED THIS IN WRITING**
before it happened: *"it is a decision about whether an empty slot beats a
sashay, not a pattern fix, so it is written down rather than taken."* It is
still that decision and it is still not taken. Recommendation on the record:
an EMPTY slot beats a sashay — a missing transition is invisible, a man
pirouetting into a walk is a visible absurdity. Queue: `walk-start-sashay`.
