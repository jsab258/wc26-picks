# Ruling: queue 147, the composed telling, the clause the bank never had, and two collision readings. LAND WITH AMENDMENTS

> **STATUS: LIVE, verified 2026-09-08.** A reference, not a plan: a ruling read
> clause by clause by the resident and the next builders, so the scannability
> cap does not apply. Director ruling at spawn
> 2026-09-08T22:45:28Z (`.claude/agent-log.tsv` line 388) on the uncommitted
> tree, branch `claude/game-dev-ai-automation-2h67ix`. Scope: queue 147 and the
> ue-probe files in section 0. NOT in scope and not reviewed:
> `tools/ue/import_prop_meshes.py` and `.github/workflows/ledger-mesh-import.yml`,
> where a second builder is live; its emit sites were read for question 6 only.

VERDICT: LAND WITH AMENDMENTS. The port is faithful, the instrument is honest,
and the builder reported against itself rather than rounding up. Six amendments:
two block the commit and are dictated text a resident may hand-apply, four block
a dispatch, and they split across two different dispatches.

The most consequential finding is not in queue 147. Section 7: the drainage
grate is specified 13.4 mm BELOW the top of the continuous channel slab that
spans it, so Jafar's item 2 cannot be satisfied by this placement whatever
collision the asset carries.

## 0. What was read, what was run

NO SHELL IN THIS SEAT. A studio-director spawn carries Read, Glob, Grep, Write
and WebSearch, so nothing below was run by me. Recorded so no session repeats the
instruction: "run the binary yourself" is not executable from here.

Read whole, each cited at the section that uses it: `StreetVoice.cs`;
`StreetVoice.h`; `crime-probe-test.cpp`; `CrimeProbe.h` and `.cpp`; `verify.py`
794 to 900; `clip-from-frames.py` 150 to 280; all 24 bank rows; the bank spec;
`ue-crime-memory-n2.md`; `VignetteShot.cpp`; `vignette-pieces.json`; the walk
verdict's prop and collision keys; and the earlier port ruling.

THE RESIDENT'S NUMBERS, QUOTED: both g++ compiles exit 0, and `crime-probe-test:
153 check(s), 0 failure(s) over 1 live bank, 133 selftest row(s)`, identical
under `-fno-exceptions`; 17 headers under `Public/`, 12 compiled by a container
test (5 directly, 7 transitively), 5 carrying Unreal types by design.

WHAT I COULD NOT ANSWER. All seven questions are answered, with two limits. I did
not run `python3 ledger/verify.py`, so `2851 over 4 of 4` is UNVERIFIED BY ME and
is amendment A2. And section 7's burial is arithmetic on a spec file, not a
frame, so A6 orders the measurement rather than the fix.

## 1. The premise check, first

The moat is social memory, consequence persistence and information. A telling
BUILT from the rumour the mill carried is the moat; a row selected from a bank by
arithmetic on a clock is not. The change moves the right number, the late-analog
framing is untouched, nothing was purchased and no instrument or threshold moved.

## 2. Question 1. Is the acceptance met

Queue 147: "The second NPC's reply is a bank pick at the achieved rung.
StreetVoice.Exchange composes a reply from what was actually carried, which is
the rung above a pick. acceptance: the reply is composed rather than picked, and
the verdict names which."

The item is imprecise about which half of Exchange composes, and the source
settles it: `StreetVoice.cs` 285 to 295 marks ONLY the telling Composed, and
says in the same comment why marking both would put a false renderable hole in
the structural bucket. Exchange has never composed the answer. An item cannot ask
a ported function to do what the function does not do, so the question is whether
the rung the item reached for was reached.

It was, and the named defect is gone. Before: the second NPC's line was
`BankPick(bank, "overheard", rung, seed)`, one of three rows filed at the rung the
memory reached. After: the answer is a literal from the band the HEARER'S OWN
DISPOSITION selects (nerve-with-a-sensitive-rumour, then loyalty, then greed,
then plain), seeded with who is replying, `seed*97+31+FNV1a(replierId)%9973`. A
different axis from the bank's rung and a better one: twelve bank rows answer one
vignette, four disposition bands answer every rumour the town will ever carry.

MET, at the rung Exchange defines, and the builder's 1 of 2 is the honest count
and the right thing to print. It is not the top rung: the next one is queue 159,
the answer composes too, which must happen in `StreetVoice.cs` FIRST, because the
C# is the behavioural definition and a port that improved something would make
the two engines incomparable. Its own SIMULATION change, its own escalation, not
tonight. AND 147 DOES NOT CLOSE TONIGHT, for rule 6 rather than quality: the
container test proves the call in the container, and a landed crime verdict
carrying `overheardReplyMode=COMPOSED/...` proves it in the build. 147 closes on
that verdict, with A3 in it.

## 3. Question 2. The sentence, on the real string

What a player hears, from the resident's run:

    You hear all sorts. He looked straight at me before he ran. Couldn't tell
    you his name, but I've got his face now, apparently.
    God. And here?

I derived the same telling by hand before that run, which locates the defect in
the data and not in the port: seed 43, confidence 0.45 so the whisper band,
`43 % 14 = 1` so template 1, "You hear all sorts. {What}, apparently."; `Trim`
drops the trailing stop; `Cap` is a no-op on a capital H.

THE ANSWER IS BETTER THAN WHAT IT REPLACED and is accepted as it stands. "God.
And here?" is a neighbour hearing that a crime happened on this street. The bank
row it replaced is better writing and is keyed to one rung of one vignette; that
trade is named on the verdict as `overheardPickWouldHaveSaid`, the right way round.

THE TELLING IS NOT ACCEPTABLE, AND IT IS NOT A TASTE QUESTION. The suggestion
that this become a card to Jafar with a default, causally true and clumsy against
better English about nothing the player did, is refused: the dilemma is
manufactured by one defect and disappears when the defect is fixed. No choice
between two wrong outputs is owed to the owner. `StreetVoice.cs` 752 to 771
states the contract, a `Rumor.Summary` is a LOWERCASE CLAUSE written to be
spliced, and all twelve `witness_summary` rows are finished first-person
sentences with capital first letters and interior full stops.

THE CONTRACT HAS TWO CONSUMERS AND THE SECOND ALREADY SHIPPED A CORRUPT
ARTIFACT. `Gossip.h` 586 to 588 appends "I heard from " + DisplayName + " that "
+ Summary, and `production/d1-probe/ue-crime-memory-n2.md` line 6 is committed:

    [D1 12:00] (0.36|heard) I heard from the shopkeeper that He looked straight
    at me before he ran. Couldn't tell you his name, but I've got his face now.

That predates queue 147. The batch did not introduce the defect; it surfaced it,
measured it
(`overheardSummaryShape=sentence-not-clause/upper-first-and-interior-full-stop`)
and pinned it, which is what rules 3b and 4 ask for.

THE SPEC IS WHERE THE CONFLATION WAS WRITTEN, so it is part of the fix.
`dialogue-crime-witness-v1.md` 23 to 27 says the text "is used twice at runtime:
as the heard memory line (Gossip.cs 394) and as her half of the overheard
exchange, so it has to read naturally both as a private record and as a thing
said aloud". The mechanism is wrong: in both roles the string is SPLICED, after
"that " in one and into a template in the other, and one string cannot be both a
finished first-person sentence and a spliceable clause.

AND THE FIX IS A WRITING TASK, NOT A LOWERCASE. Lowercasing the first character
yields "I heard from the shopkeeper that he looked straight at me before he ran",
where "me" is now the shopkeeper inside the lad's own memory: `SummaryShape` would
report `clause` and the sentence would be wrong in a way no shape check can see.
The clause must move to third person, "he looked straight at her before he ran,
and she would know his face again", after which both splices read correctly.
Amendment A3; it blocks the crime dispatch, not the commit, and the round trip
costs the same carrying one change or six.

## 4. Question 3. The caption that would burn the wrong sentence

CONFIRMED, and worse than stated. `clip-from-frames.py` 207 to 224 resolves
`lineId` against the bank and returns `bank_lines[line_id]`. Neither `lineText`
nor the `~` unescape appears anywhere in that file. So the telling frame would be
captioned with the bank row, a subset a reader could mistake for a trim; and the
answer frame would be captioned "A face you'd know is worth more than most talk
in here. Keep it in mind." while the beat said "God. And here?", two sentences
with nothing in common. That is a picture that lies about the thing it is evidence
for, which is rule 4. `SeqKeyLine` already carries `lineText=` tilde-escaped and
`lineTextSource=` per beat, so the data is on the line and only the reader prefers
the wrong field. Amendment A4. IT BLOCKS THE CRIME DISPATCH, and not the commit,
because nothing is captioned until a run produces frames.

## 5. Question 4. The edit to the studio's own gate

The finding is TRUE and the resident verified it against HEAD independently: the
committed `binaries` tuple names three sources and none includes `CrimeProbe.h`,
while `CrimeProbe.h` 13 to 24 asserted that g++ compiles and RUNS everything in
it before any dispatch. That sentence described an intention, and
`LedgerCrime::Selftest()` ran only on Jafar's PC behind a 25-minute round trip.
The builder also corrected the header's own sentence, which is rule 1 applied to
a comment its code change invalidated.

THE EDIT IS ACCEPTED. A fourth binary running the decision layer against the
committed bank is this project's stated pattern for a tool that checks the project
itself: the live file is the accepting fixture, the rejecting fixtures in
`Selftest()` are synthetic, so doing the work the tool prompts cannot break it.

THE DOUBLE COUNT IS FIXED, mechanism checked rather than report believed.
`verify.py` 890 to 896 applies two regexes to every line and sums both;
`crime-probe-test.cpp` 69 to 75 prints the inner total as "CrimeProbe.h selftest
rows: %d run, %d failed", which matches neither, and its own summary at 203
matches the first only. No line in the four binaries matches both. TWO RESIDUAL
HAZARDS: the loop has no `continue` between the regexes, and the convention that
keeps them disjoint is documented in the test file rather than beside the
regexes; and the total is a SUM over matching lines rather than last-wins per
binary. Dictated comment A1b, and queue 164.

## 6. Question 5. The six deviations and the harness that cannot be re-run

The test is the one the earlier port ruling set and it is not re-opened: is there
an input the C# defines on which the two engines now answer differently, and if
so does a golden row exist that would go red on it.

- 9, TEMPLATES PLUS `Render`. ACCEPTED. Substitution is a single pass and
  inserted text is never rescanned, so a summary carrying the literal `{what}`
  cannot recurse. Correctness reduces to whether each of the 42 templates chose
  `{what}` or `{What}` as the C# chose `what` or `Cap(what)`. I DIFFED ALL 98
  LINES BY HAND against `StreetVoice.cs` 146 to 283, content AND ORDER, including
  the two certain-band lines the C# leaves leading with the story: identical
  throughout. Order is load-bearing, the index being `seed % 14`.
- 10, THE BANDS FACTORED OUT. ACCEPTED. Boundaries identical, `>= 0.8`, `>= 0.5`,
  strict `> 0.65` in the C#'s own order, one copy of each.
- 11, THE `ExchangeTrace` OUT-PARAMETER. ACCEPTED. Nothing in `Exchange` branches
  on it. `ComposeOverheard` does branch on `T.bComposed`, which equals
  `Lines.size() == 2`, so the probe is redundant rather than divergent; named so a
  later reader does not take "no branch depends on it" for the caller.
- 12, ASCII `Trim`, `Cap` AND `Hash`. ACCEPTED, consistent with deviation 4 of
  the earlier ruling, denominator corrected. A defining input exists: a summary
  opening on a non-ASCII lowercase letter, or a non-breaking space. The offered
  measurement, 0 non-ASCII bytes in the dialogue bank, covers `Trim` and `Cap`.
  It does NOT cover `Hash`, which reads REPLIER IDS, a different set. Measured
  this session: `canon.md` carries 0 non-ASCII bytes (1 file examined), the set
  the cast ids derive from. Dictated comment A1c.
- 13 and 14, uint32 ARITHMETIC AND NO NULL `Summary`. ACCEPTED. The C# wraps
  where the C++ would be undefined and the two agree on two's complement; the
  empty-string guard has the same outcome on every defined input.

THE HARNESS IS REFUSED AS EVIDENCE, and not for being one-shot. The precedent is
one-shot emission: `ledger/PerceptionGolden/Program.cs` emits from the real Core,
the rows are COMMITTED in `ue-probe/perception-golden.txt`, and
`core-port-test.cpp` consumes them under g++ here. Producer in the repo, rows in
the repo, anyone can re-run it. A scratchpad console has neither half.

WORSE, THE HEADER CLAIMS IT IS REPRODUCIBLE. `StreetVoice.h` 16 to 19 says "the
street_voice scenario in `ledger/PerceptionGolden/Program.cs` has the REAL
Ledger.Core emit the composed sentences". There is no such scenario:
case-insensitive grep for streetvoice across `ledger/` returns 11 files and
`PerceptionGolden/Program.cs` is not among them. A future session would believe a
cross-check exists and would not build one. That sentence must not be committed:
amendment A1.

THE SMALLEST REPRODUCIBLE THING, for queue 160: a `street_voice` block in
`PerceptionGolden/Program.cs` emitting one row per template over two summaries, a
lowercase clause and one opening on a proper noun so `Cap`'s no-op branch is
exercised, plus the four answer bands and a negative seed; rows committed; a
consumer loop in the container. `dotnet` runs here, since the earlier ruling's
section 8 required a `dotnet run` of that project. It does not block tonight: the
hand diff covers the strings, the deviations cover the arithmetic, and the record
says what is machine-checked, which is none of it yet.

## 7. Questions 6 and 7. Two collision readings, and the buried grate

QUESTION 6. Both emit sites read. The street side is `VignetteShot.cpp` 932 with
`MeshCollided, Meshes`, where `MeshCollided` increments once per MESH whose asset
reports more than zero aggregate elements (`PropCollisionPrims`, 474 to 486). The
importer's own stat string at `import_prop_meshes.py` 535 reads
`propCollisionPrimsStat=saved-meshes-with-at-least-one-simple-primitive`. Both
count MESHES, neither counts primitives, over two populations: placed components
against saved assets. The name is wrong on both sides independently of the clash.

RULED: THE STREET SIDE IS RENAMED. It is inside my scope; it has one emit site and
one doc line against the importer's ten selftest assertions, a workflow key list,
four verdict files and three production documents; and it is the side with no
container test pinning it, because the string is built in the `.cpp`, against the
25 August rule that measurement formatting lives where the tests run. Rename and
move are one amendment, A5:

    propPlacedWithCollision=N/M
    propPlacedCollisionUnread=N/M
    propPlacedCollisionStat=placed-prop-mesh-components-whose-asset-reports-collision
      /over-placed-prop-meshes/PROXY-only-a-sweep-answers-whether-a-capsule-is-stopped

Whether the importer's name is also a misnomer is that builder's review. Stated
here so its director has the finding: Prims names a count of meshes on both sides.

QUESTION 7, AND THE ANSWER IS NOT THE ONE EITHER SIDE EXPECTED. First,
`propCollisionEnabled=QueryOnly/the-walk-path` IS NOT A SWEEP and answers
nothing: `VignetteShot.cpp` 940 picks that literal from `bInteractive`, the same
bool that drove `SetCollisionEnabled` at 513, so it restates the code's intention
and cannot fail. Two numbers from one variable, wearing a verdict key.

Second, the complex-as-simple finding is real, and the same eight-line function
has two more faults beside it. `PropCollisionPrims` returns 0 both when there is
NO body setup and when a body setup holds zero aggregate elements, so "no
collision object" and "collision by a complex trace flag" read identically. And
the last landed walk run prints `propCollisionPrims=0/0` with
`propCollisionUnread=0`, a zero over a zero denominator, while
`propCentreWorstMm` on the same line correctly prints `on=nothing-measured/of=0`.

Third, and this is why it blocks: the same run prints `propsAsMesh=0/23`,
`propStandIns=23/23` and
`propFallbackWhy=prop_drainage_grate_01_0=no-uasset-for-drainage_grate_01`. The
mesh route has never placed one prop mesh in the walk build, so the false NO has
not happened yet, and the FIRST run on which it can happen is the run that
produces Jafar's clip. A reading that cannot tell no collision from nothing
measured, asked for the first time on the run that is the deliverable, is section
4's failure again. A5 blocks the walk dispatch.

A SWEEP IS THE NEXT RUNG AND IT IS NOT A BLOCKED WALK. The walk probe's pair is
real and is against a wall: `collisionBlockedWallName=east_parade_bay0`,
`collisionBlockedDistanceCm=100.0`, `collisionBlockedStatus=STOPPED`,
`collisionStatus=REAL`. It says nothing about any prop. Queue 163 asks for a
capsule sweep against the piece's own component, printing the blocking hit's
actor name, because that reports collision without the character being stopped.

AND IT CANNOT REQUIRE THE CHARACTER TO BE STOPPED. From `vignette-pieces.json`,
with `y_m` a centre and `sy_m` a full height, which two dustbin rows confirm (bin
centre 0.395 height 0.61, top 0.70; its lid centre 0.725 height 0.05, spanning
0.70 to 0.75):

    ground_east_channel      y -0.221766  sy 0.300  top -0.0718  spans x 0 to 42
    gully_dish_east          y -0.255     sy 0.300  top -0.1050
    prop_drainage_grate_01_0 y -0.0925    sy 0.015  top -0.0850  bottom -0.1000

The grate's top is 13.4 mm BELOW the top of a continuous 42 m channel slab that
spans x=12, and its bottom is 5 mm above the dish floor, itself inside that slab.
The 1.43 degree cross-fall accounts for one or two millimetres, not thirteen, and
there is no CSG here: nothing cuts a hole in the channel above the gully. So as
specified the grate is buried inside the channel geometry, a capsule at x=12
stands on the channel and never on the grate, and a frame cannot show it.
JAFAR'S ITEM 2, "the drainage grate as a real mesh WITH COLLISION in a walk
clip", CANNOT BE SATISFIED BY THIS PLACEMENT even after the importer lands the
asset. That is arithmetic on a spec file and not a frame, so A6 orders the
measurement rather than the fix. It is the mirror of the placement rule in
`instruments.md`, distance to the datum plus whether the datum exists under the
footprint: `propCentreWorstMm` measures the distance and is blind to burial,
which is how a piece sits 0.00 mm from where the file put it and cannot be seen.

## 8. The amendments

A1. `StreetVoice.h` 16 to 19: DELETE the claim that a `street_voice` scenario
checks this port against the other engine. Dictated replacement for the sentence
beginning "The 98 lines of dialogue below":

    The 98 lines of dialogue below were EXTRACTED FROM THE C# MECHANICALLY
    rather than retyped, and all 98 were diffed against StreetVoice.cs 146 to
    283 for content AND ORDER by the director ruling of 2026-09-08, BY READING.
    NO MACHINE CHECKS THEM YET: there is no street_voice scenario in
    ledger/PerceptionGolden and therefore no golden row that would go red if a
    template changed. Queue 160 owes one row per template over two summaries,
    one a lowercase clause and one opening on a proper noun.

Applied by the resident as dictated text. BLOCKS THE COMMIT.

A1b. `ledger/verify.py`, beside the regexes at 890 to 896, dictated:

    # TWO SHAPES, SUMMED PER LINE AND ACROSS LINES. A line matching both would
    # count twice, and a binary printing two summary lines adds rather than
    # replaces. The convention that keeps them disjoint lives in the test files
    # (crime-probe-test.cpp 69 to 73, written after a total that counted one
    # binary's rows twice). Queue 164 makes this last-wins per binary.

A1c. `StreetVoice.h` deviation 12, append, dictated. A1b and A1c are both the
resident's hand-applied text and neither blocks:

    Hash reads REPLIER IDS, not summaries, so the bank measurement above is not
    its denominator: canon.md carries 0 non-ASCII bytes (1 file examined,
    2026-09-08), which is the set the cast ids derive from.

A2. The resident runs `python3 ledger/verify.py` and pastes the footer FROM
`ledger/.verify-footer` plus the `ue-probe instruments ok (N checks over 4 of 4
binaries)` line from THAT run. If it does not read 4 of 4, or if N is not the
committed total plus the 153 the binary printed, STOP and re-escalate: that is a
disagreement about a number, not a rounding question. BLOCKS THE COMMIT.

A3. Queue 157, the clause field. Twelve `clause` values on the `witness_summary`
rows of `content/dialogue/crime-witness-v1.json`, THIRD PERSON, lowercase, no
interior full stop; `BankPick` reads it; `CrimeProbe.cpp` 1184 files the clause as
the rumour Summary and keeps the sentence as the spoken witness line;
`dialogue-crime-witness-v1.md` 23 to 27 corrected to say the summary is SPLICED in
both roles and that one string cannot be both; the selftest prints all twelve
spliced memory forms so a reader can judge person agreement, which no shape check
can see. Applied by a builder, clauses written rather than derived. BLOCKS THE
CRIME DISPATCH.

A4. Queue 158, the caption. `caption_for` prefers `lineText`, un-tildes it, falls
back to the bank only when `lineText` is `none`, and names the source it used in
the reason. Selftest rows, accepting case first: a row with `lineText` captions
from it; a row with `lineText=none` and a valid `lineId` still captions from the
bank; `heard=yes` with neither still refuses. Applied by a builder. BLOCKS THE
CRIME DISPATCH.

A5. Queue 161, the street-side collision reading. Rename to the three keys in
section 7; make it three-valued, YES, NO, UNKNOWN, with a body setup plus either
a simple primitive or a complex-as-simple flag with geometry under it counting
YES and no body setup counting UNKNOWN rather than NO; print the words `nothing
measured` at a zero denominator; carry PROXY on the stat; and MOVE the tally, the
arithmetic and the string into the tested header with rows both ways in
`ue-probe/tests/vignette-spec-test.cpp`. Applied by a builder. BLOCKS THE WALK
CLIP DISPATCH, not the commit and not the crime dispatch.

A6. Queue 162, the buried grate. Before the walk clip is offered as item 2, the
run prints the grate's placed world-bounds top against the channel surface height
at x=12 and whether any piece occupies the footprint above it. If the 13.4 mm is
real, the fix is a gap in the channel slab at the gully or the grate rising to the
surface, a street-spec change with its own review. Applied by a builder,
measurement first. BLOCKS THE CLAIM that the walk clip shows item 2; it does not
block the walk dispatch, because the run is how the measurement gets taken.

## 9. Filed by name, not done here

- 157 the bank owes a clause as well as a sentence (A3).
- 158 the caption burns the bank row the beat no longer speaks (A4).
- 159 the hearer's answer composes too, in `StreetVoice.cs` first, then ported.
  The next rung for the overheard beat, with its own escalation and golden rows.
- 160 a golden row per `StreetVoice` template, emitted by
  `ledger/PerceptionGolden` and consumed in the container.
- 161 the street side cannot tell no collision from nothing measured (A5).
- 162 the grate sits under the channel it drains (A6).
- 163 a sweep names the piece that stopped the capsule, since a sunk gully grate
  can never stop one.
- 164 `ue_probe_tests` sums summary lines where it should take last-wins per
  binary.

## 10. The quality ladder at close

The telling: best available once A3 lands, the ceiling above it being an LLM
making this eloquent per cast member, which the C# docstring names as a later
job. The answer: first working, next rung queue 159. The cross-engine check:
first working, next rung queue 160. The street's collision reading: below first
working today, which A5 fixes. No aspect closes with a blank rung.

<!--RULING spawn=2026-09-08T22:45:28Z-->
