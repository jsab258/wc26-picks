STATUS: LIVE, verified 2026-09-09.

# Ruling: the proof that dies with the container, three judgment calls, and the quantity a predicate cannot be written without

LAND. THE COMMIT MAY GO AND THE BRIEF GOES WITH IT. Nothing in the uncommitted
586 lines is false about the world or publishes a number nobody measured, and
the morning brief has been waiting since about 06:00 on a channel whose last
receipt was 2026-09-07T11:19:36Z. Four corrections are owed, B1 to B4 below,
and NONE of them blocks this commit, because this push does not start run 36.

WHY THE ORDERING IS FREE, and it is the fact the whole ruling turns on. I read
.github/workflows/ledger-probe-unreal.yml lines 41 to 45 this session: the
Unreal probe fires on `workflow_dispatch` and on a push whose paths touch
`production/d1-probe/DISPATCH`, and that sentinel is not in the uncommitted
set. So run 36 needs a SECOND push, the runner checks out whatever the branch
points at when it starts, and every correction below lands in that commit at
zero cost to Jafar's brief. B1 to B4 BLOCK THE DISPATCH, not the commit.
What this push does fire: publish-glance (its paths at lines 69, 100 and 110
cover production/queue/**, ue-probe/Source/** and production/d1-probe/**,
concurrency group publish-glance, cancel-in-progress true, scoped to itself)
and the supervisor task workflow whose comment is the edit under review.

Decided by the studio director spawned 2026-09-09T04:51:29Z, read-only, no
shell. Everything below is either read out of a file I opened this session and
named at the line, or computed in this seat from numbers that were, and says
which. Premise checked first (CLAUDE.md section 0): a drainage grate, a kerb
and a pedestrian guard railing in a late-analog British port street, canon.md
untouched, no drift to correct.

## 1. A3 ships without its harness. The proof is not what run 36 rests on.

RULED: SHIP A3. Do not land the harness first.

The reason is not cost, it is what rests on what. A3 changes no decision: the
vote still selects, the grid is counted and not voted, and I checked that at
the code rather than in the brief (WalkProbe.cpp line 950 sets bClear from
R.SubjClear alone, line 965 casts the grid AFTER it, and the comment at 962 to
964 says so). Rule 5b's planted case is owed when a conclusion rests on the
instrument. Tonight nothing does. The moment a selection predicate READS the
grid, the grid becomes the thing that decides the shot, and at that moment the
planted case is owed before the predicate, not after. That is the gate, and
production/queue/175 is where it sits.

What would have been traded for landing the harness first is the one thing a
director is supposed to buy with the night: run 36 is the cheapest decisive
measurement available, and it tests four falsifiable predictions the 03:55
ruling wrote down before the run (rows 00 and 01 refused with a RAIL piece
named, row 02 taken, its control rectangle off frame, and the vote against the
81 cells on the chosen row). Delaying that to ship a test of arithmetic I can
check by reading inverts the order.

AND I CHECKED IT BY READING, so the arithmetic is not taken on trust tonight.
GrateSubjGridSpacingCm (line 810) returns kGrateSubjHalfCm * 2 / (N - 1) =
11.0 * 2 / 8 = 2.75 cm, the loop at 824 to 830 steps the same function, and at
I = 8 the target is C.X - 11 + 22 = C.X + 11, the far edge: 81 cells over the
CLOSED 22 cm square, both edges included. G.Cells is incremented in the loop
(line 834), so the printed denominator can never exceed the cells walked.

THE HONEST LIMIT OF THAT CHECK, stated so nobody upgrades it later: a director
reading an expression is not a test, it catches today's value and guards
nothing against tomorrow's edit. That is exactly what 175 buys, and it is why
175 is not tidying.

AMENDMENT TO 175, NON-BLOCKING. Its acceptance must be THE MOVE into a header
a container compiles, and the cut-out-by-script harness must NOT outlive the
move. As filed, lines 26 to 28 praise cutting the functions out of
WalkProbe.cpp by script and including them; that mechanism is scaffolding, and
a text cut whose markers drift silently tests a stale copy, which is the
silent-instrument failure the 25 August rule exists to prevent. Keep the 17
checks, including the planted bar, and delete the cutter when the functions
live in the tested layer. Two more items belong in 175 and are named in
section 3 below.

## 2. The three judgment calls: keep, keep, and the decline is upheld

(a) grateCandSubjGridStat: KEEP. A ruling's list of KEYS is not a cap on the
stat strings the instrument rules require, and the rule it satisfies is the
first one in .claude/rules/instruments.md: say what the number is a statistic
OF. The builder's reason is the right one and it is checkable at the line: the
candidate line prints grateCandSubjGridClear and grateCandSubjectRaysClear
side by side (WalkProbe.cpp 975 and 983), and without the stat a reader has
two coverage numbers on one row and nothing saying which one selected the
standpoint. A key that can be misread as the decision, beside the key that IS
the decision, is the cheapest possible way to publish a false gate.

(b) grateCandSubjGridFirstBlockedCellAlongAcross: KEEP. I verified the name
against the value: FirstI is along the channel and FirstJ across the street
(struct at 800 to 801), the printf at 968 emits I then J, and the key says
AlongAcross. The file's own precedent is grateCandOffsetAcrossAlongEyeCm. The
ruling asked that the blocker be named; WHERE it was met is what separates a
bar lying across the middle of the picture from a corner clipped by a post,
and that distinction is the entire reason the grid was built. One key.

(c) grateRayLiftCm on AIMED and REFUSED-OCCLUDED only: THE DECLINE IS UPHELD,
and the builder understated what it declined. As the file stands, the
SPAWN-FAILED line (1064 to 1066) and the NO-CONTROLLER line (1085 to 1087)
print no counts AT ALL: no grateCandidatesTried, no grateRaysTraced, no
grateSubjGridRaysTraced, and both skip reasons end in the reserved words
`nothing-measured` even though the candidate loop has already run and
GGrateCandLines is printed on its own at 1448 to 1457. A verdict that carries
a full candidate table and a line saying nothing was measured is rule 3b
inverted. It was RIGHT not to fix that in a printing-only turn: the fix adds
format arguments to two branch lines in the one translation unit that cannot
be compiled locally, and a compile error there costs the whole of run 36.
Goes to 175 in those words. I make no claim about whether those two lines
predate tonight; I have no shell and did not diff them.

## 3. The series is enough to READ and not enough to WRITE A PREDICATE FROM

RULED: one more quantity is owed before anyone writes a selection predicate,
and it is NOT the one the obvious guess would print.

FIRST, THE PROVENANCE OF 18.7 AND -180.0. No run has printed them. Run 36 has
not happened, and the only thing that has compiled A3's arithmetic is the
scratch harness 175 describes. They are predictions of the same class as the
03:55 director's trigonometry, which that ruling refused to act on in its own
words, and they may not enter any record as measurements.

SECOND, THE STRUCTURAL GAP, which needs no run and is the part that matters.
The printed angle is to the control rectangle's CENTRE, and the in-frame test
at lines 1217 to 1219 reads FOUR EDGES built by taking the control centre's
projected point and expanding it by the SUBJECT rectangle's projected pixel
half-width and half-height (1213 to 1216, deliberate, "same area by
construction"). So the control box is not its own projection, and the natural
extra quantity, the control rectangle's own angular extent, IS THE WRONG ONE:
a predicate built on it would pass its own test and still clip.

WHAT IS OWED: the SUBJECT rectangle's angular half-extent from the candidate
standpoint, per axis, as grateCandSubjHalfPitchDeg and grateCandSubjHalfYawDeg
with a stat naming it as the half-extent the in-frame test adds around the
control centre. The predicate is then closed form with no tunable:
abs(OffPitch) + SubjHalfPitch <= HalfV and abs(OffYaw) + SubjHalfYaw <= HalfH.
It needs no player controller, so it can be computed in the candidate loop
where no camera exists yet, and it belongs in the same builder turn as the 175
move so it lands in the tested layer the first time.

WHY THE MARGIN CANNOT BE WAVED THROUGH, arithmetic computed in this seat from
the table's own offsets and labelled as mine and unmeasured: from row 01
({85.0, 0.0, 120.0}) the distance to the control point is sqrt(165^2 + 120^2)
= 204 cm, the line to it makes atan(120/165) = 36 degrees with the ground, so
the 22 cm subject extent projects to about 22 x sin(36) = 12.9 cm across the
pitch axis, a half-extent of about 1.8 degrees. IF row 01 prints 18.7 against
a 20.0 half-field, the 1.3 degree margin is SMALLER than the half-extent the
test will add. The key as printed cannot answer the question a predicate would
ask it. Run 36 still settles the ruling's prediction, because grateRectStatus
is measured directly on the chosen row and is not inferred from this angle.

THIRD, -180.0 IS CORRECT AS PRINTED and needs nothing. WrapDeg180 (852 to 857)
leaves exactly plus or minus 180 unchanged, so the sign at the boundary is
whichever way the subtraction landed and is not a direction. A predicate must
read the absolute value, which the form above already does, and a control
point behind the camera then needs no special case: it is not a near miss, it
is a certainty of off-frame, and the same inequality says so.

## 4. B1 to B4: owed before the dispatch, dictated text, no compile risk

B1, RESIDENT, DICTATED TEXT. THE VERDICT HEADER PUBLISHES TWO CLAIMS THIS
STUDIO REFUTED TONIGHT. Lines 1324 to 1330 are inside WriteFinalVerdict, which
is the reason A1 blocked for the occluder name at the same site, and they say
the pair exists because the face is coincident with the channel AND carriageway
top faces and that whether THAT ties is read by tools/grate-zfight.py. Two
faults. The tool reads a 22 cm rectangle wholly inside the CHANNEL course, so
the carriageway strip is unmeasured (amendment 2,
decision-2026-09-09-grate-camera-and-zfight.md, which already forbade exactly
this sentence at exactly this reach). And the flicker half cannot fire on a
static pair at all (production/queue/173: a deterministic tie reproduces
identically, a flicker of zero is what a stable tie looks like). I found this
by grepping the SENTENCE and not the site, per rule 1: "coincident" has four
hits in this file, at 161, 162, 275 and 1325, and 1325 is the published one.
Replace 1324 to 1330 with, keeping the Out.Add(TEXT("# ... ")) shape:

      and no frame showed. The PAIR exists because the piece's top face is
      coincident with the channel and carriageway top faces over about 0.16
      square metres (0.1599, decision-2026-09-09-the-grate-rises-flush.md).
      WHAT IS ACTUALLY READ IS THE CHANNEL HALF: tools/grate-zfight.py
      measures a 22 cm rectangle spanning z 2.76 to 2.98, wholly inside the
      channel course, so the crown-side strip where the grate meets the
      CARRIAGEWAY, about 2.60 to 2.745, is UNMEASURED by any rectangle in this
      run (amendment 2, decision-2026-09-09-grate-camera-and-zfight.md). AND
      THE FLICKER HALF CANNOT FIRE ON A STATIC PAIR: both frames come from a
      camera that did not move over static geometry, so a depth-test tie is
      deterministic and reproduces identically, and a flicker of zero is what
      a stable tie looks like (production/queue/173). So this run carries ONE
      reading, the speckle density against the same-area control rectangle
      named on the grateRect line, and one named gap. THE CONTROL IS THE
      DENOMINATOR: no bound is set here and this file decides nothing about
      the tie. It supplies the two frames and the two rectangles as FRACTIONS
      of the frame.

And the unpublished copy at 160 to 170, same two faults, replace with:

      TWO FRAMES OF ONE STATIC CAMERA, AND WHAT THE SECOND ONE CAN AND CANNOT
      SHOW. The grate's top face is coincident with the channel and carriageway
      top faces over about 0.16 square metres (0.1599, the flush ruling), and
      tools/grate-zfight.py reads a 22 cm rectangle wholly inside the CHANNEL
      course: the carriageway strip, about z 2.60 to 2.745, is unmeasured by
      any rectangle here. The second frame was taken for a frame-to-frame
      flicker density, and production/queue/173 establishes that a static
      camera over static geometry cannot produce one: the tie is
      deterministic, so a flicker of zero rules nothing out. THE PAIR STAYS
      because it costs one capture and the pair that CAN differ is queued;
      until 173 lands, the flicker half is a named gap and not evidence.

B2, RESIDENT, DICTATED TEXT. grateSubjGridRaysStat IS LESS SELF-DESCRIBING
THAN THE KEY IT WAS MODELLED ON, on the one point that matters. The numerator
is cumulative over the standpoints actually tried; the denominator GridBudget
(line 875) is kGrateStandCount x 81 = 648, the whole list. grateRaysStat
discloses that with its third clause, "the-search-stops-at-the-first-clear-one"
(1108); the grid's stat drops it (1024 to 1026 and 1110 to 1112), so a chosen
row 00 prints 81/648 and reads as 567 rays lost. In BOTH values replace
`81-cells-each/counted-apart-from` with
`81-cells-each/the-denominator-is-every-standpoint-in-the-list-and-the-search-stops-at-the-first-clear-one/counted-apart-from`.
String only, inside existing TEXT() literals, no format argument moves.

B3, RESIDENT, DICTATED TEXT. THE REFUTED NAME STILL CARRIES THE REASON THE TIE
READING IS VOID. I grepped "barrier" in production/NOW.md: three hits, lines
17, 25 and 30, of which 25 is the correct use and 17 is the original claim a
log entry properly preserves. Line 30, and queue/172 line 35, say the run
"measured speckle on a barrier post against asphalt", which is the wrong object
in the sentence that does the work. Replace at both sites with:

      It measured speckle inside a rectangle crossed by the guard railing's
      post and mid rail, over a piece that renders as a near-white patch with
      almost no texture. A flat near-white patch is not a surface a speckle
      statistic can read.

B4, RESIDENT. THE 03:55 RULING'S NON-BLOCKING AMENDMENTS HAVE NO HOME, and one
tracked file already says otherwise. I grepped production/queue for
`near-white|white void|grate-zfight|rect_px`: the only hits are 172 lines 27,
39 and 173 line 4. So A6 is NOT filed, while queue/172 lines 27 to 28 state
that the near-white render "is filed separately", and A5's three grate-zfight
items are not filed either. A5(iii) is the rect_px rounding snap, which was
amendment 4 of decision-2026-09-09-grate-camera-and-zfight.md, was found
unapplied by the 03:55 ruling, and is now on its third record without ever
being carried out. File two items, numbers 176 and 177 free as of this
session: "the near road renders near-white" carrying A6's three outcomes and
the Pillow read that needs no dispatch, and "the zfight tool reads rectangles
it never checked the status of" carrying A5 (i), (ii) and (iii) with the third
marked as third-record. THE RULE THIS ENFORCES, because it has now failed twice
in one night: a non-blocking amendment is not recorded until it has a queue
number, and a close-out that does not check for the numbers is how a ruling
decays into a preference.

PRE-DISPATCH GATE, mechanical, four greps, no judgment: `173` appears twice in
WalkProbe.cpp; `the-denominator-is-every-standpoint-in-the-list` appears twice;
`barrier post` appears zero times in production/NOW.md and production/queue/172;
production/queue/176 and 177 exist. Then python3 ledger/verify.py, footer pasted
FROM ledger/.verify-footer, then the sentinel push.

## 5. The two questions asked for confirmation

THE RESIDENT APPLYING A1 AND A2 IS INSIDE THE SPLIT. CONFIRMED. CLAUDE.md
allows the resident to hand-apply dictated text or a one-line fix, the 03:55
ruling addresses A1 and A2 to the resident by name as dictated text, and the
escalation gate that covers this commit was met twice: that ruling carries
`<!--RULING spawn=2026-09-09T03:55:58Z-->` at its last line, and this record
adds a second stamp from the close-out spawn. I verified the three code sites
carry the corrected text (182, 1009, 1333) and that `crowd_control` has zero
hits in WalkProbe.cpp against the three sites A1 listed.

THE COMMENT-ONLY WORKFLOW EDIT DOES NOT BREACH THE CHANNEL RULING. CONFIRMED,
with the limit of my evidence stated: I have no shell and did not diff, so
"comment only" rests on the resident's report plus what I read, which is that
the edit region (lines 36 to 45) is comment text under a pre-existing firings
convention, and the step it fires (line 394, `--send-outbox`, bounded wait,
output to production/pc-ops/outbox-sweep.txt) exists with its own rationale
block dated to the 8th. The test is whether the push CHANGES the channel or
USES it: no step, trigger, credential or retry is added, and .claude/rules/ci.md
positively orders running the existing entry point instead of arguing about it.
THE STANDING FORM, so this cannot become a licence: a comment-only push that
fires this workflow APPENDS A DATED LINE to the firings block naming the reason.
This one does. A firing push with no such line is channel work wearing a send's
clothes.

## The ladder, asked at close

Best available or first working? FIRST WORKING, and the next rung is named and
is no longer blank: the selection predicate that weighs the PICTURE, which
cannot be written until grateCandSubjHalfPitchDeg and grateCandSubjHalfYawDeg
are printed beside the centre angles (section 3), and which may not read the
grid until the planted case ships with it (175). Rung after that, carried
forward unchanged from the earlier ruling: a control rectangle displaced along
world X so distance and grazing angle cancel between subject and control.

## What the Producer may add to what it already carries

May add: the probe now prints a dense 81-cell coverage count beside the
five-ray vote, so the next run says whether the vote is sampled or measured
instead of arguing it. May NOT say: that the sampling fault is proven in this
repository (the proof is in a scratch file and the numbers are recorded in
queue 175, which is not the same fact), that any candidate angle has been
measured (run 36 has not happened), or anything about a depth-test tie. The run
35 tie reading stays void, and the flicker half stays a named gap.

<!--RULING spawn=2026-09-09T04:51:29Z-->
