# Ruling: the regime change lands, three doors a grep cannot see, the sky with its stand-in retired, and what the Hook caption must say

STATUS: LOG, 2026-09-09. NOT CURRENT once the commit lands and the blocking
amendments in section 8 are applied; from then the files are the reading copies
and this is the record of why. Director review of the channel regime change and
the visual batch on spawn `2026-09-09T10:06:54Z` (line 451 of
`.claude/agent-log.tsv`, the newest `studio-director` row), read-only, no shell.
The coordinator's brief asked four questions (A to D) and weighed its own
reporting; this record answers the four, names what it found that was not
asked, and records Jafar's words where a LIVE file will not keep them.

VERDICT: LAND WITH AMENDMENTS. FIVE ARE BLOCKING and none of them is code: two
before the push (a hold record and a sequencing rule), three before the 04:00Z
wake (a process restart through an existing entry point, a record whose label
is false, and the Producer's own instructions). The sky stays in the batch with
its fill retirement. The Hook caption gains one sentence and loses one. The
vacuous glance check is a queue item. Nothing in the batch moves a gate to make
red go away, and no number below was printed by me: every count is the
coordinator's or a builder's, marked as such, and section 0 says what I read
instead.

## 0. What was read, what was taken on report, and his words

NOTHING WAS RUN. Read at the line this session: `tools/morning-brief.py` 1 to
90 and 1905 to 1944; `tools/map-notify.py` 1 to 80 and 868 to 897;
`tools/runner/cards.py` by grep (51 to 83, 236 to 247, 706, 736, 1364 to 1381);
`tools/runner/telegram-bot.py` 11 to 48, 955 to 994, 1095 to 1149, 1298 to
1365, 1553 to 1614, 2830 to 2868, and the selftest sites by grep;
`tools/runner/brief.py` 1 to 433; `tools/producer-day.py` 1 to 120;
`tools/runner/checkout-contains.py` 1 to 80; `tools/runner/run-night.ps1` 85
to 99; `tools/runner/install-scheduled-task.ps1` in full;
`tools/supervise.py` by grep; `.github/workflows/ledger-install-supervisor-task.yml`
1 to 110, 500 to 693; `.github/workflows/ledger-restart-telegram-bot.yml` 23
to 53; `.github/workflows/publish-glance.yml` by grep; `ledger/verify.py` 1175
to 1345; `tools/glance.py` 1478 to 1612 and 2060 to 2159;
`ue-probe/Source/LedgerProbe/Private/VignetteShot.cpp` 140 to 217, 1015 to
1074, 1200 to 1394, and the sky sites by grep; `tools/imagegen/compare-hook-2026-09-09.json`
in full; `production/watchdog-prompt.md` 41 to 180 and 228 to 257;
`production/NOW.md` 1 to 130; `production/decision-queue.md` 1 to 259;
`production/ladder.md`; `production/budget.md` 30 to 59; queue items 180, 186,
188, 189, 191; `production/briefs/2026-09-09.md`;
`production/outbox/2026-09-09-morning-brief.brief.md` and its receipt; the two
wake records; `production/pc-ops/cards-send.txt`, `telegram-bot-restart.txt`,
`telegram-bot-restart.request`, `supervisor-status.txt`; `.claude/agents/producer.md`
1 to 135; `production/findings.txt` 1673 to 1740, 2210 to 2281, 2319 to 2378,
2379 to 2440, 2441 to 2520; `production/art/atlas-01/REVIEW.md`; the two
rulings of spawn `2026-09-09T08:04:25Z`.

TAKEN ON REPORT, not read: the 14, 29, 48 and 3003 check counts; the 56 grep
hits with 9 in executable channels; the AST walk; the YAML parse of 18
workflows; that `VignetteShot.cpp` compiles in CI; the text of the live trigger
(it is not in the tree). Section 9 names what that leaves open.

PREMISE CHECK, CLAUDE.md section 0: an overcast British sky over a late-analog
port street, a 1990 Hook district board, a channel to a non-technical owner
with a phone. Nothing purchased: the imagegen lane is his PC and an allowlisted
model; the sky is an engine actor and no asset. Codex's own edit prompt had to
pull their first board "into a visibly 1990 British port rather than a
nostalgic 1930s town", which is the exact drift section 0 warns of, in their
lane, caught by their eye. Ours may show the same prior. That is part of what
the comparison measures and the caption does not hide it.

HIS WORDS, RECORDED HERE BECAUSE NOW.md IS LIVE AND GETS PRUNED. From the
verbatim block `tools/runner/brief.py` 7 to 16 carries, reading of total 25,
Fable 24, ceiling 75 (`production/budget.md` row 39, "at about 09:00Z"):

    "The channel fails because nobody with judgment sits in it. Replace the
    machinery with one judgment step." "One Producer turn a day writes the
    single message. It reads the queue, findings, decision queue, receipts
    and ladder, and decides what I see and what I never see. The brief
    generator, the cards pass and the page notifier are retired; the register
    stays as a format check after the Producer writes." "Every brief carries
    two buttons, readable and unreadable. Unreadable means tomorrow's is
    written differently, and the Producer says what it changed. This is the
    only measure of the channel: seven consecutive readable briefs, tapped by
    me, is the acceptance. Selftests do not count."

And from NOW.md 30 to 41, in the studio's transcription: the Producer applies
the director test itself (no numbers with units, no coordinates, no file names,
no studio vocabulary); it takes every decision that has a recommendation and a
default, logs it, and reports the notable ones in the Sunday summary; the six
cards on his phone are ruled as their recommendations; a card reaches him only
when the studio cannot form a recommendation, at most one a week, with buttons;
fix the cards race (queue 189) and no other channel work. Then the sky before
any wetness, the Hook comparison from the creation prompt, the Mickey's
blockout dispatched.

## 1. A. The retirement is complete in the tree and open in three places the tree is not

### 1.1 What the greps say, re-run from this seat

`morning-brief` hits 18 files: the retired tool, its banner's own quotations in
`producer-day.py`, `run-night.ps1`, `verify.py`, `producer-check.py`, `map.py`,
`gallery.py`, `queue-check.py` (all comments naming it as the thing retired or
as a former importer), four queue items, three rulings, one receipt, the
findings, the watchdog record, `push-diagnosis.txt`. `map-notify` hits 6
files: the tool, two comment lines in `publish-glance.yml` (424, 439) saying
the job is gone, `map.py` comments, the notified record, queue 134, one ruling.
`sweep_cards|send_cards|cards.py` hits 11 files, and inside `telegram-bot.py`
the only call to `sweep_cards` outside the selftest is the method's own body,
which raises (979 to 987); the loop at 1358 to 1360 calls `flush_inbox` and
`sweep_outbox` and nothing else. The refusals are real and first:
`morning-brief.py` 1917 to 1928 prints and returns 5 before reading anything,
after `--selftest` only; `map-notify.py` 879 to 889 the same; `--send-cards`
2844 to 2851 loads no credentials and returns 5. The coordinator's claim holds
at every site I opened.

The places a grep cannot see that I COULD check from the tree: the supervisor
scheduled task runs `launch-supervisor.py` which runs `supervise.py` whose
children are the watcher, the bot with no arguments (`supervise.py` 646 to
649, `START THE TELEGRAM BOT.bat` 95) and the executor; none invokes a retired
tool. `run-night.ps1` no longer calls the generator (85 to 99) and its README
says `-Register` adds a 23:30 scheduler entry; whether that entry exists on
his PC is unknowable from here, and it would now run a loop that calls nothing
retired. The launcher `.bat` files call no retired tool. The wake records name
none.

### 1.2 Door one: the process on his PC is yesterday's code, and his tap tomorrow lands in it

`production/pc-ops/telegram-bot-restart.txt` records the bot restarted at
`2026-09-08T13:14:35Z` on `botSourceSha256=72D11D0A...` with `stableHead`
614bddb2. `supervisor-status.txt`, read at 3cf3731 at 10:42Z today, says
`telegram-botUptimeSec=70063`, which is 19.5 hours, so the running bot IS that
restart: it started before the cards loop existed and before
`brief.parse_callback` existed. Nothing restarts it on a code change:
`supervise.py` restarts a child on exit, not on a changed file (grep: every
restart site is about crashes and the wait ladder).

TWO CONSEQUENCES. First, `handle_callback` in the running process has only the
card parser. When he taps readable or unreadable on tomorrow's brief, the old
process receives `b|2026-09-10|R`, `cards.parse_callback` refuses it, he is
told "I could not read that tap" and nothing is recorded (today's code at 1133
to 1140 shows the refusal path the old code will take). The ONLY measure of the
channel would be lost on its first sample. Second, until this batch lands, a
crash-restart on his PC starts the bot from his checkout at 3cf3731 or later,
and at 3cf3731 the loop swept cards every 120 seconds off a WAITING section
that held seven cards, five with receipts and two without (the ladder and the
last bus). `telegram-botStarts=4 telegram-botStops=3` says it has crashed
three times in two days. A crash tonight sends him two cards the studio has
since taken, against his ruling of this morning.

RULED, B3 in section 8: after the batch lands AND the PC's checkout is proven
to contain it, the bot is restarted through the existing entry point
(`production/pc-ops/telegram-bot-restart.request`, which fires
`ledger-restart-telegram-bot.yml`), and the evidence file is read: `stableHead`
at or after the batch commit, `botSourceSha256` after different from
`72D11D0A506A5A2C1DA45630D46E80CC463E2D4830C26901FB3F9043D49516CC`,
`restartAction=restarted`. Before 04:00Z. This is operating the channel, not
working on it, and `.claude/rules/ci.md` says the entry point is cheaper than
the argument.

### 1.3 Door two: the committed HEAD still carries the machinery, and pushes fire it

The batch deletes the cards step and the loop call, but the branch's HEAD has
both, and `ledger-install-supervisor-task.yml` fires on any push touching
`production/outbox/**`, `production/briefs/**`, `production/decision-queue.md`
or its own plumbing (79 to 98). A workflow runs from the commit that was
pushed, so any push BEFORE the batch that touches those paths runs the old
step, reads his checkout's WAITING, and sends the two unreceipted cards.

RULED, B2: the batch is the next push that touches any of those paths. Nothing
is written into `production/outbox/` or `production/briefs/` and
`decision-queue.md` is not pushed separately before the batch lands. The
batch's own push runs the new workflow, which has no cards step.

### 1.4 Door three: the archive sits at the path the new sender reads, and the batch's own push runs the sender

`production/briefs/2026-09-09.md` is the RETIRED GENERATOR'S output, kept as an
archive (findings 1697 to 1700: "kept at production/briefs/2026-09-09.md,
labelled as not the brief that went") whose label was then removed because the
register refused it (1711 to 1736). It reads "NEEDS YOU: When does the last bus
leave Meridian?", a question the studio has since taken (decision-queue 83 to
86). The brief he actually received today went through the OUTBOX at 04:28Z,
message 39, no buttons, receipt under slot `2026-09-09-morning-brief.brief`.
`BriefReceipts.state` (brief.py 354 to 372) asks for slot `brief-2026-09-09`,
which does not exist, so `brief_pass` with no day (1577) reads
`production/briefs/2026-09-09.md`, finds it unsent, runs the register on it
(which it passed when generated: `registerFindings=0`, findings 1680), and
SENDS IT WITH TWO BUTTONS. The batch's push touches the workflow file, which is
in its own path filter, so the new brief step runs on landing. If that is
before 00:00Z, the first two-button brief he ever sees is the machinery's
text, naming a question the studio has answered, and his tap on it starts the
streak on the thing he retired.

The fix is the mechanism's own and is four lines of someone else's code called
once, not a change: `BriefReceipts.hold(day, clause)` (391 to 396) writes a
refusal record with `held=True`, which `state` reads as `held` (368 to 371) and
`send_brief` refuses (261 to 264). RULED, B1 in section 8, with both outcomes
watched. The streak starts 2026-09-10 with the first brief a Producer wrote.

### 1.5 Door four: the recorded copy of the daily prompt says "as received" over text that was not

`production/watchdog-prompt.md` 41 to 78 describes the 09:47Z rewrite in seven
steps and does not carry it verbatim. The block at 146 is headed "THE PROMPT AS
RECEIVED 2026-09-09T04:09:00Z, whole" and its line 160 reads "ruled 2026-09-09,
the generator is retired", which no prompt received at 04:09 could have said;
lines 118 to 120 admit "the two run lines below were updated that day". So the
file holds a hybrid under a false label and the verbatim live prompt nowhere,
and the live prompt's own last line (175) tells tomorrow's session to compare
what it reads against this file and write any difference into NOW.md. It will
find differences that are this file's, not the trigger's. That is the decayed
claim in the one instruction every daily session obeys, the fault this very
file records at line 85 about the budget sentence.

RULED, B4: the coordinator, who set the 09:47Z text, pastes it verbatim under
the heading "The prompt as set 2026-09-09T09:47Z, verbatim", and the 04:09
block either gets its two lines restored to what was received or its heading
changed to say, in those words, that two lines were edited at 09:47 and the
received text is not recoverable. A record may be incomplete; it may not be
labelled as something it is not.

### 1.6 Door five: the Producer's own instructions do not carry the regime

`.claude/agents/producer.md` describes the 2026-09-03 register and says NEEDS
YOU carries "two to four options, a recommendation, a default". It does not
carry the director test in his words (a grep for `units`, `coordinates`, `file
name`, `studio vocabulary` finds nothing), nor that the studio takes decisions
with a recommendation and a default, nor that a card is at most one a week, nor
that the message lives at `production/briefs/<day>.md` with two buttons, nor
that an unreadable tap means tomorrow's is written differently and says what
changed. Tomorrow's brief is the first Producer turn under the regime and it
runs under this file. RULED, B5: a section of his words, dictated in section 8,
before 04:00Z. The register below it is unchanged; the check still enforces
what it enforces.

### 1.7 What I could not check, named rather than assumed

Whether a 23:30 `run-night` task is registered on his PC. Whether the trigger's
live text matches anything. Whether anything outside this repository on his
machine double-clicks a retired tool; the `.bat` files in the tree do not.

## 2. B. The sky: in scope, with the stand-in's retirement, and the order of suspicion written before the frame exists

RULED: THE FILL RETIREMENT IS THE SAME CHANGE, NOT A SECOND ONE RIDING IT, and
it stays. Three facts from the code decide it.

(a) THE FILLS WERE THE SKY'S STAND-IN BY THEIR OWN DECLARATION. The constant
block at 143 to 151 says the three directionals are Unity's trilight ambient
"made with the tool this engine has, it needs no captured sky", and queue 186's
own measurement names them `ambientModel=trilight-3-directional/not-a-captured-sky`.
Adding the captured sky while leaving the stand-in lit is not "add a sky"; it is
two ambient models in one scene, which the file's header calls the fault this
project has paid for twice (1219 to 1223).

(b) THE RETIREMENT IS TECHNICALLY COUPLED, NOT ADJACENT. A directional light is
an atmosphere sun light by default in this engine, so three lit fills put extra
sun discs into the sky the atmosphere renders (1241 to 1246). Zeroing them is
what keeps the atmosphere honest; a sky with three suns in it is not a test of
the sky.

(c) IT IS GATED AND READ BACK, NOT ASSUMED. `FillScale` is 0 only when
`SkyIsWhole()` (1239, 1247), which asks both actors for both components at the
moment of the question (1208 to 1213); a failed spawn leaves the fills lit and
the fog opaque (1275), so today's frame comes back rather than a black one.
`fillsRetiredToZero` is read off the light's intensity, never predicted from
the rule (1360 to 1371), and the scene line prints it beside `skyWrites`,
`SkyIntensityRead`, `FogMaxOpacityRead` and the four cvars (1503 to 1509).
That is the instruments rule kept: the number is a statistic of the light, and
a disagreement between rule and light is exactly what the key exists to show.

THE PRICE, accepted and named. The first frame moves two lighting variables at
once and an under-lit frame cannot say which. No measurement can separate them
before the dispatch because nothing here compiles. So the order of suspicion is
written now, BEFORE the frame, and is binding on whoever reads it:

1. READ THE FRAME (rule 4) and the sky segment line together. `skyWhole`,
   `fillsRetiredToZero`, `skyWrites` against `applyCalls`, `fogMaxOpacity`,
   and the sky band series (`skyTop`, `skyCentre`, `bandGroundOverSky`) are
   what say whether the mechanism ran. A far field still reading 0.98 with
   `fogMaxOpacity=0.45` printed is a different fault from one with 1.0 printed.
2. If the frame is BLACK or the build fails on the four atmosphere setters
   (1296 to 1299), the builder's mechanical fallback applies: delete those four
   lines; the sky spawns at engine defaults and reads back. That is a compile
   fallback, not a tuning move.
3. If the frame is DIM but the sky segment says whole and retired, the first
   knob is `kSkyIntensityDay` (left at the engine default 1.0 deliberately, 187
   to 195), read against the sky band series. The fills do NOT come back as a
   knob; the finding at 2411 to 2413 says the sky must come DOWN from 0.98
   toward the reference's 0.81, so re-lighting the stand-in is the wrong
   direction by measurement.
4. Nothing in `kSky*` or `kFogMaxOpacityWithSky` moves until a series of at
   least two frames has been printed, per rule 2. The code already says the
   four atmosphere numbers "are a starting point and say so" (175 to 182) and
   the fog opacity is "derived, with its unknown named" (196 to 206). That is
   the right register and it binds the next session too.

WHAT IS NOT IN THIS CHANGE AND CORRECTLY SO: wetness, which reaches nothing
(queue 186 status, three declarations and no read), and which Jafar ordered
after the sky. The finding's ceiling is honest and I adopt it: a sky gives the
dry road a wide dim wash, not the mirror in Codex's panel, and `roadSpread` and
`roadCv`, not `roadMeanLuma`, are what to read for whether anything is being
reflected (2417 to 2422).

THE DISPATCH IS ITS OWN PUSH with the sha captured first, watched by ancestry,
per `.claude/rules/ci.md`, after core-tests has finished, and after the art
request if the resident fires that first (NOW.md 54 to 70 says the art lane
yields to a game workflow and loses the run). Which order is the resident's,
but not both in one push.

## 3. C. The Hook caption carries both halves, the card is his, and the pictures go as pictures

### 3.1 The caption

The spec file is already honest about what it is: `compare-hook-2026-09-09.json`
records the three passes, the eye selection, the 1024x1536 against 688x1024,
the distilled plate model asked for a board (file comment 35 to 44,
`their_image.not_one_draw`, the item note 291 to 304). The finding at 2344 to
2352 says what the caption must do, and I ratify it: THE CAPTION CARRIES BOTH
HALVES OR THE COMPARISON IS RIGGED AGAINST US ON THE EXACT QUESTION HE IS
DECIDING. Theirs was drawn, corrected twice after a person looked, and chosen
by eye, discards uncounted; ours is the first draw, chosen by nobody, at under
half their size. Neither clause flatters us. Together they are what happened.

RULED, substance for the Producer, who keeps the register and the 120-word cap
(the B5 text of the morning ruling is at the cap, so one sentence goes to make
room: the correction-note sentence, which the batch-of-fifteen headline
already told him):

    Theirs went through three passes, corrected twice after someone looked
    and chosen by eye. Ours is the first draw, nobody chose it, and it is
    drawn at under half their size. Three more of ours exist if you want
    them; a favourite of four against their one board would be a different
    comparison again, and the message would say so.

No "fixed seed", no "pixel count", no model names: those are studio vocabulary
under his director test. "Under half their size" is the honest plain form of
45 per cent.

### 3.2 Whose decision it is, and why rule 2 does not take it

His rule 2 has the studio take every decision with a recommendation and a
default, and the morning ruling's B6 carried both (B, outside for boards, until
ours reads as close). Three things keep this one his. His item 4(a) of 07:00
said in terms "that comparison decides whether concept images are made in
house": he reserved the decision to his eye on two pictures, and the later
general rule names the six cards on his phone and does not repeal a specific
reservation he made two hours earlier. Constitution law 9 forbids the studio
self-certifying quality on a taste dimension, and "does our board read as a
usable concept sheet" is one. And the studio's recommendation is about the
EVIDENCE (a plate model asked for a board), not about the picture, which it
has not seen. So this is the case rule 2 reserves: the studio cannot form the
recommendation the question needs. IT IS THE WEEK'S ONE CARD, sent when the
pictures exist and not before. B6's options stand, and its DEFAULT B protects
him if he does not answer.

### 3.3 How it reaches him this week, decided under rule 2 and logged

Three of his sentences cannot all hold this week: one message a day; images
inside the brief; no channel work beyond queue 189. `send_brief` sends text
with a keyboard (brief.py 277 to 278, telegram-bot.py 1596 to 1598) and
carries no picture; `sendMediaGroup` does not exist (grep over `tools/runner`:
no hit); the only card sender raises. Putting a picture or a card keyboard on
the brief is channel work. TAKEN BY THE STUDIO, with recommendation and
default, logged under the new section (A3 in section 8): the two boards go as
two Telegram pictures through the outbox's existing photo path (proven
2026-09-08 by the first-crime photo message), Codex's first, ours second with
the caption on the second saying it is the second of two; the same push
carries the brief, whose NEEDS YOU poses the question in words and says
"reply A or B"; the sweep step runs before the brief step in the same
workflow run (539), so the pictures are on his phone before the words that
point at them. His typed answer reaches the next Producer turn through the
dossier (`producer-day.py` 43 to 44 lists his messages since the last tap)
and is logged as RULED BY JAFAR. The buttons he named are the NEXT RUNG: a
one-shot that sends exactly one named card with `cards.keyboard_for` through
the existing tap path, and a photo sidecar on the brief, both built the first
week rule 4 is not in force or the day he says so, whichever is first. Queue
item A6. He is told this in one plain sentence in the brief that carries the
card and nowhere else: the buttons are not built yet, so reply A or B.

### 3.4 The licence question is taken, not carded

Queue 188 ("may the concept boards ship") has a recommendation and a default
(A, withhold from the site, reference only) and is therefore TAKEN under rule
2, logged under TAKEN BY THE STUDIO (A4). The research half stays a research
task with no card: a builder reads the service's terms as of the date and
records whose account made the boards; if the terms plainly permit shipping
the studio takes B under a decision record naming them, and if they are
ambiguous that is "cannot form a recommendation" and it is that week's card.
Sending `hook.png` to his own Telegram is review of his commission's
deliverable and needs nothing, as the batch ruling already said.

## 4. D. The green check that tests neither half of its name is a queue item, and the commit says so

READ AT THE LINE. `aBrokenImportKeepsTheCountAndSaysTheWords` (glance.py 2134
to 2143) renders `model["looks"][1]`, the LIVE dashboard look, beside a
synthetic broken-import reason. With the live count at 0 the renderer takes
its zero branch (1178: "nothing measured, 0 card(s) under WAITING"), so the
words it asserts come from the empty-queue path and not from the broken-import
path, and the finding at 2492 to 2499 measured the "top item" hit as the
provenance note's prose (`topItemSubstring=1 topItemCommaLine=0`). The check
cannot go red while WAITING is empty, and WAITING is empty by construction from
today. The three sibling checks that went RED were fixed by planting
`FIXTURE_QUEUE_THREE_PUSHABLE` (1536 to 1595); this one passed and so was not.

RULED: QUEUE ITEM, NOT A BLOCKER, for two reasons I checked rather than
reasoned. `glance.py` has no row in `TOOL_SELFTESTS` (verify.py 1177 to 1211:
ten rows, none is the glance), so no footer and no commit claims it as
evidence; it runs as a gate on the PUBLISH only (publish-glance.yml 287 to
291), where a vacuous check weakens the gate by one property rather than
passing something false. And the property it guards is a rendering of a
non-empty needs-you count under a broken import, which the live page with
zero cards cannot exhibit either way. The fix is one instrument-builder turn
and is dictated (A5). The builder stopping rather than fixing was correct:
the instruction was to stop.

THE DENOMINATOR THE COMMIT OWES: the commit message quotes the glance
selftest's own count line and adds, in words, that one of those checks
measured nothing on an empty WAITING section and is named in queue 192. A
clean count that includes a check that examined nothing is a zero without its
denominator (rule 3b) until that sentence is beside it.

## 5. What else the batch carries, read rather than reported

(a) THE THREE SELFTEST ROWS ARE NOW WIRED TO THEIR OWN TOOLS. `TOOL_SELFTESTS`
indices 7, 8, 9 are `checkout gate`, `brief`, `producer day` (1208 to 1210),
and `checkout_gate_selftest`, `brief_selftest`, `producer_day_selftest` call
`_tool_selftest_run(7)`, `(8)`, `(9)` (1323, 1334, 1344). Correct today.
Queue 191 (look up by name, refuse a missing name) stays open and is the
right fix; the positional interface is the trap, not the counting.

(b) THE CHECKOUT GATE IS IN ALL THREE SENDING STEPS (sweep 460, brief 617,
flush 762) and resolves from the RUN's checkout, so the new file must be
tracked or both senders refuse for ever; the `checkout gate` verify row makes
that red here first. The refusal is printed, not a job failure (631 to 643),
with both shas named, and the exit codes split stale from cannot-prove (the
tool's 3 and 4). That is the shape queue 189 asked for.

(c) TAKEN BY THE STUDIO is correctly written: seven taken with their
reasons, and the pages card recorded as RESOLVED BY EVENTS rather than taken
(decision-queue 88 to 95), which is right because the studio decided nothing
there. Two more go in by this ruling (A3, A4).

(d) THE WAKE RECORD `faed9289` IS DUE AT 16:00Z TODAY for A1 of the batch
ruling (the executor's WAKE_DRAIN opt-out). The coordinator's CHANGED list
does not name `wake-drain.sh` or `executor.py`, so A1 does not appear to be
in this batch; I did not verify. If it has not landed by 16:00Z the record
comes due and the drain blocks one Stop to say so, which is the mechanism
doing its job and not a fault. The resident reads the record rather than
this paragraph.

(e) THE OUTBOX HOLDS MESSAGES WRITTEN THIS MORNING UNDER HIS OWN ITEMS: the
atlas-02 digest he ordered under 4(c), the grate correction whose headline
the batch ruling's B1 dictated, the map-changed message the retired notifier
wrote before it was retired. The batch's push sweeps them. The regime begins
with tomorrow's brief; messages he asked for this morning go. The
map-changed one is the notifier's and the Producer may hold it by the same
refusal-record mechanism B1 uses if it judges so; I do not rule on it.

(f) `producer-day.py` writes the dossier to `production/brief-input/<day>.md`
for a Producer that has Read and no Bash. Whether the dossier shows the TAKEN
section so the Sunday summary can be written from it is unproven: on the first
real run the resident reads the dossier for those headings and files a gap if
they are absent. Rule 6: the Sunday summary is built when something reads the
section it summarises.

## 6. The coordinator's own reporting, weighed as asked

Four refuted claims, each corrected at every greppable site, is the discipline
working, and the three selftests wired to their neighbours were caught by
reading labels rather than results, which is the right instinct and is now a
queue item so the next person does not need it. The pattern across the four,
named by the coordinator itself, is the tidy story: two days stale, never
served, reads as ironwork, withdrawn five hours earlier. Each was a sentence
that sounded like a measurement. The corrective that worked every time was one
command. Section 1 of this record is the same pattern from the other side: the
grep said nothing calls the retired tools and the grep was right; the doors
were in a process, a commit, a path and a label, which is what the rule about
open lists decaying like comments means.

## 7. The ladder, asked at close

THE CHANNEL: FIRST WORKING and unproven; the harness exists and has sent
nothing; the acceptance reads 0 of 7 with no tap ever recorded, in those
words. Next rung: tomorrow's brief, written by a Producer, sent with buttons
by a bot that can read the tap. THE SKY: BUILT AND UNRUN; next rung is the
dispatch and the sky band series, then the HDRI the shared file names (the
code already prints whether the file is reachable), then wetness. THE HOOK
COMPARISON: the spec is BEST AVAILABLE for a fair one-draw comparison in this
lane; next rung is the run on his PC and, after his answer, the concept kind
with its own measured settings. THE CARDS RACE: FIXED IN THE TREE and unproven;
next rung is the first evidence file printing both shas. THE DECISIONS: TAKEN;
next rung is the first Sunday summary. THE GLANCE CHECK: a blank next rung
until A5 lands.

## 8. Amendments

B1, BLOCKING, resident, BEFORE THE PUSH. Hold today's archive so the new sender
cannot send it. From the repository root, one line:
`python3 -c "import sys; sys.path.insert(0, 'tools/runner'); import brief; print(brief.BriefReceipts('.').hold('2026-09-09', 'retired-generator-archive-sent-without-buttons-at-0428Z-through-the-outbox-before-the-ruling-streak-starts-2026-09-10'))"`.
Then both outcomes watched, accepting first:
`python3 -c "...; print(brief.BriefReceipts('.').state('2026-09-10'))"` must
print `unsent`, and the same for `2026-09-09` must print `held` with the
clause. Open the record it wrote under `production/outbound/` (rule 4), stage
it BY NAME, quote both state lines in the commit message. The archive stays
where it is, unlabelled, as findings 1729 to 1732 ruled.

B2, BLOCKING, resident, sequencing. The batch is the next push that touches
`production/outbox/**`, `production/briefs/**`, `production/decision-queue.md`
or the supervisor workflow's own files. Nothing is written into the outbox or
briefs directories before it lands. Know what the push fires (rule 9): the
supervisor workflow (its own file), core-tests (`tools/*.py`), publish-glance
(`tools/glance.py`), and not the restart.

B3, BLOCKING BEFORE 04:00Z, resident, through the existing entry point. After
the batch lands, read the batch run's `production/pc-ops/outbox-sweep.txt` or
`brief-send.txt` for the checkout gate's PC head sha: when it contains the
batch commit, edit `production/pc-ops/telegram-bot-restart.request` to
`reason=the-running-bot-predates-the-brief-tap-parser date=2026-09-09` and push
it ALONE. Then read `production/pc-ops/telegram-bot-restart.txt`:
`restartAction=restarted`, `stableHead` at or after the batch commit,
`botSourceSha256` after differs from `72D11D0A506A5A2C1DA45630D46E80CC463E2D4830C26901FB3F9043D49516CC`.
ARM THE CHECK IN THE SAME TURN as the request push (rule 8):
`python3 tools/wake-queue.py arm --due <push instant plus one hour> --by director/2026-09-09-regime-ruling --instruction "Read production/pc-ops/telegram-bot-restart.txt: restarted, stableHead contains the batch, botSourceSha256 changed. If not, push the request again and say why in NOW.md."`
If the gate prints REFUSED-STALE-CHECKOUT on the batch run, wait one watcher
pass and re-read; do not push the restart onto a checkout that does not yet
contain the code.

B4, BLOCKING BEFORE 04:00Z, coordinator, `production/watchdog-prompt.md`.
Under section "The daily trigger was REWRITTEN 2026-09-09", add the heading
"The prompt as set 2026-09-09T09:47Z, verbatim" and paste the text as set,
whole. Then the block headed "THE PROMPT AS RECEIVED 2026-09-09T04:09:00Z,
whole": either restore its two edited run lines to the received text, or
change the heading to "THE PROMPT AS RECEIVED 2026-09-09T04:09:00Z, with two
run lines edited at 09:47Z; the received text of those two lines is not
recoverable from this file". Lines 118 to 120 already say the edit happened;
the heading must agree with them.

B5, BLOCKING BEFORE 04:00Z, resident, `.claude/agents/producer.md`, dictated
text, his words and nothing of mine. A new section after "The caps":

    ## Ruled by Jafar 2026-09-09: one turn a day, and the director test

    "One Producer turn a day writes the single message. It reads the queue,
    findings, decision queue, receipts and ladder, and decides what I see
    and what I never see. The brief generator, the cards pass and the page
    notifier are retired; the register stays as a format check after the
    Producer writes." "Every brief carries two buttons, readable and
    unreadable. Unreadable means tomorrow's is written differently, and the
    Producer says what it changed. This is the only measure of the channel:
    seven consecutive readable briefs, tapped by me, is the acceptance.
    Selftests do not count."

    The Producer applies the director test itself: no numbers with units,
    no coordinates, no file names, no studio vocabulary. It takes every
    decision that has a recommendation and a default, logs it under TAKEN
    BY THE STUDIO in the decision queue, and reports the notable ones in
    the Sunday brief. A card reaches him only when the studio cannot form a
    recommendation, at most one a week. NEEDS YOU therefore normally says
    that nothing needs him.

    The message lives at production/briefs/<YYYY-MM-DD>.md, one file per
    day, sent unchanged with its two buttons. The dossier the director
    hands you is production/brief-input/<day>.md, gathered by
    tools/producer-day.py, which judges nothing; the judgment is yours.
    After an unreadable tap, the next brief says in one sentence what was
    written differently.

A1, resident, decision-queue.md, under TAKEN BY THE STUDIO, dictated:

    ### TAKEN 2026-09-09 BY THE STUDIO, AS RULED: A. Withhold from the site;
    reference only.
    Was: "May the concept boards ship?" (queue 188). The allowlist has no
    2D-image entry and PROCESS 1 withholds untagged output. The terms are
    still to be read; if they plainly permit shipping the studio takes B
    under a decision record naming them, and if they are ambiguous that is
    the week's card.

A2, resident, decision-queue.md, under TAKEN BY THE STUDIO, dictated:

    ### TAKEN 2026-09-09 BY THE STUDIO, AS RULED: the two Hook boards go as
    two pictures in the same morning as the brief, and the card goes inside
    the brief as words with a typed answer.
    Was: three of his sentences of 2026-09-09 (one message a day, images
    inside the brief, no channel work beyond queue 189) cannot all hold
    while the brief sender carries no picture and no card sender exists.
    Recommendation and default: pictures as pictures through the existing
    outbox photo path, Codex's first, ours second, the brief pointing at
    them; the button sender and the photo-in-brief are the next rung, built
    the first week the channel is not frozen. Notable: report in the Sunday
    summary.

A3, Producer, the Hook caption, when the pictures exist: section 3.1's
substance, the correction-note sentence removed, the register kept. The card
per B6 of the morning ruling, inside the brief, with "reply A or B; the tap
buttons are not built yet" as its only sentence about mechanics.

A4, queue item, named: "the one weekly card has no button sender". One-shot
`--send-card <id>` sending exactly one named card with `cards.keyboard_for`
through the existing tap path, receipt per card, no sweep; and a photo sidecar
on the brief in the shape `PHOTO_REF_SUFFIX` already uses. Both refused this
week by rule 4; the item records that and waits.

A5, queue item 192, instrument-builder, one turn: in
`aBrokenImportKeepsTheCountAndSaysTheWords` replace the live look with a
planted dashboard look whose count is derived from
`FIXTURE_QUEUE_THREE_PUSHABLE` (never typed), assert that count appears in the
rendering beside the broken-import words and that no card element renders;
add the rejecting case (cards half working, broken-import words absent) so the
check can go red both ways; print `countKept=N/N words=present cards=0`. The
six honest zeros the finding names print the words "nothing measured" beside
their denominators. Close the gap the finding leaves open:
`zeroWaitingShipsItsDenominator` against the live page bytes too.

A6, queue item, named: "the sky's first frame is read before any number
moves", carrying section 2's four-step order so the session that opens the
frame reads the rule and not its memory of it.

A7, resident, commit message: the glance count line plus the sentence section
4 owes; the two `state` lines from B1; the sky's unrun status in words
("compiles only in CI; the dispatch is its first test; the fills retire only
when the sky is whole and the scene line prints which happened").

PRE-COMMIT, mechanical: B1 applied and both lines printed; B2 honoured; A1 and
A2 written; the three new tools staged by name; `python3 ledger/verify.py`,
footer from `ledger/.verify-footer`. Then the push. Then B3 and its arm. Then
B4 and B5, committed as documents on the resident's read, before 04:00Z. The
sky dispatch and the art request as their own pushes, in the order NOW.md 54
to 70 requires, neither in the batch.

## 9. What this spawn did not get to

I did not read `wake-drain.sh` or `executor.py` for A1, so section 5(d) is a
reading of a record, not of code. I did not read `outbox.py`'s `render_refusal`
or `refusal_rel`, so whether B1's clause survives in the filename or only in
the body is the resident's to see when the record is opened; the `state`
printout is the proof either way. I did not open `production/d1-probe/DISPATCH`.
I did not read the C++ body of `LedgerVignette::SkySegment` behind the 3003
count. I did not count the B5 caption against `count_words`. And I have seen
no frame with a sky in it, because none exists.

## 10. Nothing goes to Jafar today

No card. The week's one card is the Hook comparison, sent when the two pictures
exist, and it was his by his own sentence before rule 2 was spoken. The one
thing in this record that bends one of his words is the card going without
buttons this week, and he is told that in one sentence inside the brief that
carries it. Everything else here is the studio keeping its own house before
the first morning of the regime he ordered.

<!--RULING spawn=2026-09-09T10:06:54Z-->
