LEDGER — the exported speech graphs, read from the files themselves
ran on Jafar-Desktop (Windows) as Jafar, 2026-08-11 08:19 UTC
graphs looked for in C:\Users\Jafar\wc26-picks\tools\voice-live\game-out

        text export: 2026-08-11 08:14 UTC  finished  src=bfb1238e4718 — step 9.9e-07/1.7e-06, prefill 1.6e-06
        decode export: 2026-08-11 08:19 UTC  finished  src=f62df6c368bb — 1.1e-04 traced, 8.0e-05 untraced, 540 MB
  ok    t3-step.onnx is on disk   [2001 MB, 2001 MB with weights]
  ok    t3-prefill.onnx is on disk   [2029 MB, 2029 MB with weights]
  ok    the prefill still takes 'text_tokens' — it was not folded away into a constant   [text_tokens, speaker_emb, cond_speech_tokens, emotion_adv]
  ok    the prefill still takes 'speaker_emb' — it was not folded away into a constant   [text_tokens, speaker_emb, cond_speech_tokens, emotion_adv]
  ok    the prefill still takes 'cond_speech_tokens' — it was not folded away into a constant   [text_tokens, speaker_emb, cond_speech_tokens, emotion_adv]
  ok    the prefill still takes 'emotion_adv' — it was not folded away into a constant   [text_tokens, speaker_emb, cond_speech_tokens, emotion_adv]
  ok    the step takes a token and a position   [token, position, cache0]
  ok    the prefill returns the first token's odds, not only a cache   [logits, cache0, cache1]
  ok    and its 60 cache outputs are exactly the step's cache inputs   [prefill out 60, step in 60, step out 60]
  ok    every input has a type this tool knows how to feed
        types: text_tokens=tensor(int32)  speaker_emb=tensor(float)  cond_speech_tokens=tensor(int64)  emotion_adv=tensor(float)  |  token=int64, position=int64
  ok    3 voices available to compare   [ada, crowd_f1, crowd_f2]
        voices: ada -> crowd_f1 one field at a time (emotion nudged) — speaker_emb=3.825  cond_speech_tokens=3.416  emotion_adv=2.529;  ada twice differs by 0.0e+00
  ok    changing 'speaker_emb' alone changes the cache — that part of the voice is an input, not baked in   [3.8247]
  ok    changing 'cond_speech_tokens' alone changes the cache — that part of the voice is an input, not baked in   [3.4158]
  ok    changing 'emotion_adv' alone changes the cache — that part of the voice is an input, not baked in   [2.5295]
  ok    and the same voice twice gives the same cache   [0.0e+00]
  ok    with no infinities or NaNs in it
        position: 1 vs 2 and 17 move the answer by 7.359
  ok    the position changes the answer — it is an input, not baked into the graph   [7.3586]
  ok    and the prefill's odds are the same width as the step's — one array feeds one sampler   [prefill 8194, step 8194]
  ok    the prefill's cache drives the step three times, growing by one each   [44 -> [45, 46, 47]]
  ok    and the answer it gives is a number
  ok    the decode graph still takes 'tokens'   [tokens, prompt_token, prompt_feat, embedding, z, sine_noise]
  ok    the decode graph still takes 'prompt_token'   [tokens, prompt_token, prompt_feat, embedding, z, sine_noise]
  ok    the decode graph still takes 'prompt_feat'   [tokens, prompt_token, prompt_feat, embedding, z, sine_noise]
  ok    the decode graph still takes 'embedding'   [tokens, prompt_token, prompt_feat, embedding, z, sine_noise]
  ok    the decode graph still takes 'z'   [tokens, prompt_token, prompt_feat, embedding, z, sine_noise]
  ok    the decode graph still takes 'sine_noise'   [tokens, prompt_token, prompt_feat, embedding, z, sine_noise]
        decode: ada/6tok=5760smp  ada/15tok=14400smp  crowd_f1/6tok=5760smp  crowd_f1/15tok=14400smp
  ok    the decode graph runs at two token counts and two voices, and the sample count follows the formula each time
  ok    and it produces a waveform rather than silence or NaN   [peak 0.0333]

RESULT: all clear
