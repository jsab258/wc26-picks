line: production (the console)
spec: Jafar's Director's Console rulings, 2026-09-03
acceptance: a health panel carrying laws and gates red/amber/green from the weekly process audit; lessons this week AND HOW EACH TERMINATED; Blocking pushes this week; Producer versus resident disagreements; and the studio-versus-game split of points
max_sessions: 1
status: READY 2026-09-03, after the Monday reset. instrument-builder.

## Three of the five inputs already exist

The process audit is `production/queue/900-process-audit.md`. Lessons and their
terminations are `ledger-v2/studio-v2/learning.md`. The studio-versus-game
split is ALREADY MEASURED and printed every run as `gameShareDay`, how many
spawns built the game rather than measuring or reviewing it. On 25 August it
read 32 of 110, so 71 percent of the studio's effort was the project working on
itself. That number belongs in front of Jafar.

## Two do not, and both are new this week

Blocking pushes come from `production/interrupt-log.tsv`, which exists and has
no rows: `blocking-count.py` correctly prints `nothing measured` rather than
zero. Producer versus resident disagreements have no recorder at all yet, and
the same rule applies: `nothing measured`, never 0, until something writes one.

## The termination column is the point

A lesson that was filed and never terminated is a lesson nobody acted on. This
project has a standing rule that a lesson terminates as a rule edit, a gate or
a queue item, and the panel should show which, so an accumulating list of
insights cannot pass for a functioning studio.
