line: infrastructure (the morning brief)
spec: production/NOW.md, "JAFAR'S STANDING ORDER, 2026-09-05", item 1c
acceptance: the tool writes today's brief from repo state with no model call, and `python3 tools/producer-check.py --kind brief production/briefs/<today>.md` PASSES; every number in the brief names the file it was read from on the same line; two runs on one checkout produce byte-identical output apart from the generation instant; on a checkout whose newest budget row is older than 48 hours the budget line reads the words "nothing measured" instead of the stale figure, and a source the tool cannot read makes it REFUSE to write rather than emit a brief with a hole, both proven against planted fixture trees; the done line prints sourcesRead=N/M; the brief carries the studio versus game split in words in its BUDGET section, counted in sessions with the sentence that it is sessions and not points until the rate is measured, and `python3 tools/producer-check.py --kind brief` REFUSES a brief with no split sentence, proven with a planted brief; `splitBasis=` appears on the tool's done line and nowhere in the message; `tools/runner/run-night.ps1` no longer writes a brief of its own, proven by `grep -c 'Fallback brief' tools/runner/run-night.ps1` reading 0 and by staging by name; the daily trigger's prompt is recorded in the tree and names this tool; and the queue counts the brief prints and the counts the verify footer prints come from ONE counter reading `production/queue/`, printed as ready, blocked and done with the retired `game-design/queue.md` named as not read (queue 079), proven by the footer changing when a file is added
max_sessions: 1
status: LANDED 2026-09-05. production/briefs/2026-09-05.md generated by the tool and passed the register; one queue counter (moved 85 to 86 and back on a planted file); run-night.ps1 'Fallback brief' count 0; the trigger prompt is recorded. The morning push rides queue 089 and is unproven until a morning passes.

## Why a tool and not a session

Item 1c is explicit: generated from repo state by a tool, pushed by the bot
every morning, not written by hand in a session. A hand-written brief costs a
spawn, arrives when the studio happens to be awake, and carries whatever the
writer remembered. A generated one costs nothing against the ceiling.

## The register is the hard constraint and it is already law

`production/briefs/` is walked by `producer-check.py --gate`, which runs inside
`ledger/verify.py`, so a brief that breaks the register cannot be committed.
The brief register is 150 words with the full shape and a required evidence
link. Four files predate the register and are exempt by BOTH a marker in the
file and a frozen PRE_REGISTER list in the tool; that list is not for widening.

So the tool must SELECT under a 150-word cap rather than dump state. That is
the feature, not the obstacle.

A LIVE CONFLICT TO RESOLVE IN THIS TASK: `tools/runner/run-night.ps1` writes a
mechanical fallback brief ("# Fallback brief (mechanical; the rich-brief
session did not run)") into the same directory and commits it. That text does
not carry the register's shape, so the first night that writes it puts the tree
in a state the gate refuses. The fallback is generated by this same tool, one
writer; ruled 2026-09-05.

## What the brief reads, and what it may not claim

Sources, each named beside its number: the queue counts under
`production/queue/`, done/ and blocked/; the WAITING count and top card in
`production/decision-queue.md`; the newest row of `production/budget.md` WITH
ITS AGE; what landed since the previous brief, from git; and the picture
`tools/report-frame.py` offers, which it withholds when the last build measured
nothing.

THE STUDIO VERSUS GAME SPLIT IS REQUIRED BY THE STANDING ORDER AND IT IS NOT
IN POINTS YET. `production/budget.md` says the turns-to-points conversion is
UNMEASURED and that no per-tier points figure enters that file until two paired
readings exist; queue 076 is the rate that would fix it. So the brief prints
`splitBasis=items` or `splitBasis=spawns` beside the split and does not call it
points until 076 has landed. A split in a unit nobody has measured is a number
twice.

## Both halves, accepting first

Accepting: run it on this checkout, then run the register check on its output
and show the pass, then run the tool again and diff the two outputs.

Rejecting, two planted fixtures: a tree whose newest budget row is three days
old, where the budget line must read "nothing measured" rather than carrying
the old figure as current, which is exactly stop condition 2 of budget.md; and
a tree with `production/decision-queue.md` unreadable, where the tool refuses
to write at all and says which source it could not read.

## Depends on, and what it blocks

Depends on queue 089 for the morning push to his phone; the generation itself
depends on nothing and is useful the moment it lands. Related: queue 074, the
`latest.md` moving name, which this tool must not make worse.

If the 079 half does not fit in this session, it returns to item 9 and NOW.md
says the footer's ready count is not to be quoted until it lands.

FOLDED IN BY THE RULING OF 2026-09-05 (088 batch), section 5 and 6:
`ledger/verify.py` gains one checker each for `tools/runner/inbox.py --selftest`,
`tools/inbox-read.py --selftest` and `tools/runner/telegram-bot.py --selftest`,
parsing the count line each prints and red on any failure or no count line; and one
gate that refuses a commit while `production/inbox/` holds an untracked message
file, printing `inboxUntracked=N/M` and "nothing measured" on an empty folder. If
it does not fit this session it returns to the queue under its own number.
