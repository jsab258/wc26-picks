line: infrastructure (governance)
spec: this file
acceptance: CLAUDE.md under 2000 words of standing rules plus pointers, with the goal block intact at the top; nothing deleted outright, every displaced passage moved into the framework doc that already covers it; tools/goal-block-check.py still green; a director row, because CLAUDE.md is a director-trigger file
max_sessions: 3
done: 2026-09-02 by director ruling game-design/decision-2026-09-02-constitution-cut-attribution-pc-channel.md. Acceptance met: 16,291 words to 1,990 with the goal block intact and checked verbatim, nothing deleted, every destination verified to carry its passage (23 markers across 8 files), and a director row. Six dictated edits applied at landing.

CLAUDE.md is roughly 15,000 words. The roadmap's row law caps a milestone row
at 80 words on the reasoning that a document nobody can hold in their head is
a document nobody reads. The file that is read at the start of EVERY session
is the one place that rule matters most, and it is the one place the rule was
never applied. Jafar's finding, 2026-09-01.

Cut it to under 2000 words of standing rules plus pointers.

MOVE, NEVER DELETE. Almost every long passage in there exists because a
specific failure cost a specific day, and the incident is what makes the rule
believable rather than decorative. Losing the incidents would make the file
shorter and worse. Each one goes to the framework document that already
covers its subject: measurement faults to studio-v2 verification and the
instruments rules, CI and evidence-channel lessons to the ci rules, studio
and routing to operations and organization, the auto-mode operating manual to
runner.md, and anything already superseded to legacy.

WHAT STAYS IN CLAUDE.md:
- The goal block at the top, verbatim, untouched. It is checked by
  tools/goal-block-check.py and the weekly audit.
- The premise in a few lines: what LEDGER is, the era, the moat, so no
  session can invent an answer.
- The standing laws that must not need a lookup: never assert unchecked,
  every zero ships a denominator, guards tested both ways, verify before
  commit, the branch and pull-request and purchase constraints, the licence
  allowlist, the formatting law, the studio split.
- Pointers to where the detail now lives, by path.

TWO HAZARDS, both of which have already happened here:
1. A pointer to a document that does not carry the thing pointed at is worse
   than no pointer. Check each destination actually receives the passage.
2. The quality-ladder mechanism was orphaned by exactly this kind of move
   (L23): its enforcement clause stayed live in CLAUDE.md while its home was
   superseded. Before moving any passage that names a MECHANISM, confirm the
   mechanism's new home exists and the clause points at it.

A word count belongs in the verify footer afterwards, so the file cannot
quietly grow back. Do not invent a bound beyond Jafar's 2000; print the count
first and let the series argue for anything tighter.
