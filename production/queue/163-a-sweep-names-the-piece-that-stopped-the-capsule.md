line: instrument (the walk clip's evidence)
spec: A capsule sweep against a named piece's own component in the walk probe,
  printing the blocking hit's actor name, so collision is reported without requiring
  the character to be stopped.
acceptance: the verdict names the piece a sweep hit, and a sunk or missing piece reads
  as a miss with the reason rather than as a silent pass
max_sessions: 1
status: READY 2026-09-08. Filed by the director ruling
  game-design/decision-2026-09-08-queue-147-the-composed-telling-and-the-clause-the-bank-never-had.md
  section 7, as the next rung above every proxy in queue 161.
  WHY A COUNT CANNOT ANSWER IT. A primitive count is a proxy for collision and a
  three-valued collidable reading is a better proxy, but neither answers whether a
  capsule is stopped. Only a sweep does.
  AND IT CANNOT REQUIRE THE CHARACTER TO BE STOPPED. The walk probe's existing pair is
  real and is against a WALL: collisionBlockedWallName=east_parade_bay0,
  collisionBlockedDistanceCm=100.0, collisionBlockedStatus=STOPPED, collisionStatus=REAL.
  It says nothing about any prop, and a gully grate flush with a footway should never
  stop a walking person. A test that demanded it would be testing the wrong thing.
