line: infrastructure (instruments)
spec: game-design/decision-2026-09-02-vignette-batch-canon-crews-d1-timebox.md, Ruling 9
acceptance: the 123 pending keys learned in their OWN commit, only after every group traces to a real instrument; the StreetVignette prefix added to keys_in on the next touch of verdict-keys.py
max_sessions: 1
status: READY 2026-09-02. RESIDENT WORK, no spawn.

`verify.py` reports 1075 verdict keys with 123 new, pending `--learn`. That
file is evidence, so it lands in its own commit and not inside a batch.

THE CONDITION THE RULING ATTACHES: learn them only if every group traces to
an instrument written on 1 or 2 September. A key that looks PER-RUN rather
than per-instrument means stop and resume the director, because learning a
per-run key teaches the tool that a moving value is a fixed one.
