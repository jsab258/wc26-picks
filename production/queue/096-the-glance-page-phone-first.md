line: production (the console)
spec: production/NOW.md, "JAFAR'S STANDING ORDER, 2026-09-05", item 3. Carries the five ruled items from queue 068, which this supersedes.
acceptance: one self-contained page generated by a tool holds exactly the five ruled blocks in the ruled order, blockCount=5, every number naming the file it came from, zero external references (no remote script, stylesheet, font or image) and no fixed or minimum width above 360 CSS pixels, with the page weight printed against its stated cap; generated at two clocks with --now, a sentence older than 24 hours renders GREY and a fresh one does not; with a source removed, that block reads the words "nothing measured" and the overall colour goes GREY rather than green, proven by a planted fixture tree; and the page is OPENED and read before any gate number is quoted
max_sessions: 1
status: READY 2026-09-05. instrument-builder. SUPERSEDES queue 068, which the integrator should retire when this lands.

## The five things, ruled, and no sixth

1. Overall state as a COLOUR and one dated sentence, written by the Producer.
   GREY after 24 hours unrefreshed, because a stale judgement that still looks
   current is the fault this console exists to end.
2. Needs-you count and the top item.
3. Next visible thing and when, or the word `unknown`. Never a padded guess.
4. The latest image.
5. The budget bar: points to ceiling on BOTH meters, and hours to reset.

Everything else is one tap down. `STATUS.md` and `dashboard.html` keep every
derivation and denominator and become the audit level, which is right for the
surfaces agents read: constitution law 12 puts evidence beside the sentence for
agents and behind it for Jafar.

## The sentence is a judgement and the colour is derived

The colour comes from named inputs. The sentence does not: the Producer writes
it, and it MUST BE ALLOWED TO SAY WORSE THAN YESTERDAY. A console that can only
report progress is a console nobody believes twice.

## Phone-first is measurable or it is decoration

The container has no browser, so the instrument is the generated file, and it
is checked on things a file can be asked: the viewport declaration, the largest
declared width, the count of external references, the byte weight against a
stated cap, and the block count. Then somebody OPENS it, because looking is not
measuring and measuring is not looking, and this project has shipped a green
number standing in for an unopened frame more than once.

## Both meters, and the higher one governs

The budget block reads `production/budget.md` and shows total and Fable
separately with the governing one named. It never infers one meter from the
other: on 2026-09-01 Fable led by seven and on 2026-09-04 the total led by one,
so there is no rule of thumb. A row where a meter was not read shows `not
read`, never zero.

## Both halves, accepting first

Accepting: generate against this checkout, print the five checks with their
numbers, open the page, and say what is on it.

Rejecting, two planted cases: a tree with no dated sentence, which must render
GREY with the words "nothing measured" rather than a green page with a blank
line; and a page regenerated at a clock 25 hours after its sentence, which must
go GREY. Both are the same guard from its two sides, and the accepting side is
the one that goes unrun.

## Depends on, and what it blocks

Depends on nothing in the queue. Blocks queue 097, which publishes it, and 097
is what makes it a page he can actually open. Related: queue 069 (the health
panel) and 070 (show moments as dated rows), which sit one tap down and are not
part of this deliverable. Queue 047 is the staleness lesson this page inherits.
