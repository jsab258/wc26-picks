line: instruments
spec: The surface gate counts the population it is about, so that a green
  number cannot require shipping wrong content.
acceptance: a run printing the replacement keys with their denominators, and
  16/16 no longer reachable by writing a file that should not exist
max_sessions: 1
status: READY 2026-09-09 22:05Z. A gate denominator that counts the wrong
  population, which is the fault family this studio has been correcting all
  day under rule 3b: ask what the denominator COUNTED.

  THE FAULT. surfacesAsked=16 counts card and multiply, which are DECAL BLEND
  MODES and not library surfaces at all (queue 223). So surfacesResolved=16/16
  and mapsFound=48/48 are UNREACHABLE unless somebody writes card.png and
  multiply.png into the pack, and doing that would paint one shared image
  across four different shop fascias and three different interiors. THE GREEN
  STATE OF THIS GATE REQUIRES SHIPPING WRONG CONTENT, which makes 12/16 a
  correct reading that mismeasures rather than a fault to be fixed.

  THE REPLACEMENT, derived from the pieces rather than hard-coded:
    surfacesAsked=14 over non-decal pieces, each library surface statused
      RESOLVED, PROCEDURAL (no pack file, rendered from the spec tint, matching
      Unity) or ABSENT (no file and no spec entry, which IS a real fault)
    decalImagesAsked=20 decalImagesBound=20/20
    piecesUnpainted, which is 30 today and 0 when queue 223 lands
  The single key that proves the frame changed is piecesUnpainted=0 over the
  pieces examined, with surfaceStatus=PROCEDURAL printed for exactly interior
  and paint_yellow.

  IT NEEDS A RULING BEFORE IT LANDS, because changing what a gate counts
  changes a conclusion: it turns a standing 12/16 fault into a mismeasurement.
  Escalation is mechanical here and is not a judgement call.
