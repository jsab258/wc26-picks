line: world (D1 probe, measurement b, Unity half)
spec: production/d1-probe/plan.md, measurement b
acceptance: the reference street built to the Unity ceiling within the box; paired stills committed daily; no hand-edited binary scenes
max_sessions: 4

Build the agreed reference street to the Unity ceiling inside the timebox,
committing stills daily so the comparison has a dated series rather than
one final screenshot.

The street is the same one the UE half would build, so define it here in
data first: which block, which dressing, which time of day and weather.
Data-driven per D1's standing constraint, so the definition survives the
engine decision either way.

NOTE FOR THE DECISION RECORD: with the UE half blocked, this half alone
cannot decide measurement b. It establishes the Unity ceiling as a fact.
Writing it up as "Unity wins on visuals" would be comparing one number to
nothing, which is the failure this project has a rule about.
