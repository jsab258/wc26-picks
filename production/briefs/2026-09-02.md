# LEDGER brief, 2026-09-02 (written at a budget stop, not on a schedule)

## Landed

1. **CLAUDE.md, 16,291 words to 1,990.** Your finding. The file read at the
   start of every session, in a project whose own law caps a roadmap row at
   80 words on the reasoning that a document nobody holds in their head is a
   document nobody reads. Nothing was deleted: 1,316 lines moved into three
   casebooks and four framework documents, each moved line proven present by
   a checker that reads the original out of git rather than off disk. That
   checker caught its own author within the hour: the first mover read the
   working tree, so re-running it after the cut was installed silently
   rewrote every casebook with the cut text instead of the original.

2. **59 asset files stopped being credited to the wrong author.** Vehicle
   models from OpenGameArt sat inside a directory watched under a Kenney
   licence row, so the sweep folded them under Kenney's name and reported
   them compliant. OpenGameArt appeared nowhere in the attribution file. A
   licence gap reported as compliance is worse than one reported as nothing.

3. **The licence checker had four tests and all four tested failure.** The
   accepting case had never run in the life of the tool that checks licence
   compliance. Twelve fixtures now, three accepting first.

4. **The job channel to your PC is rebuilt.** Jobs by name, one writer on the
   results branch, per-job timeouts. A flat one-hour step cap would have
   killed every overnight batch on the hour and printed TIMED OUT, which is
   indistinguishable from a hang.

## The one that is worth more than it looks

A content fetch landed unattended at 00:30 and regenerated the attribution
file. The director's correction survived it, because the ruling insisted the
GENERATOR string be fixed and not only the file. A file-only fix would
already be gone, and nothing would have announced it.

## Blocked, and by what

- **The budget, and it is the only real blocker.** Your newest reading is 8
  percent, taken during the evening of 1 September. Twenty agent spawns
  landed after it, three of them the expensive director tier. Nothing in the
  container can read your usage page, so the night is UNMEASURED rather than
  young, and the standing rule for an unmeasured day is to stop. One number
  from you clears it.
- **One double-click**, whenever it suits, on `START THE STUDIO MACHINE.bat`
  at the top of the project. Not blocking. It fetches two surfaces and is
  also the only way to learn whether that channel is alive: an idle watcher
  and a dead one produce identical evidence until a job is waiting.

## Numbers

- CLAUDE.md 1990 of 2000 words, now printed in every verification footer so
  it cannot quietly grow back.
- Attribution sweep: 3848 files walked, 2715 asset files, 0 unclassified,
  after the unattended fetch added content nobody had classified in advance.
- Selftests: 12 attribution fixtures, 31 pc-watcher checks, 77 cadence
  fixtures, 48 footer-string fixtures.
- Queue: four items closed, three amended with ruled text, six opened.

## What I am NOT claiming

The cut is safer than the file it replaced and IT IS NOT YET SAFE. The
pointers can rot and the casebooks can decay, and neither has an instrument.
That is queue 020, and until it exists the cut is trusted rather than tested.
The director landed it under that condition rather than freely, and this
brief says so rather than reporting a clean win.

## Next, when the budget allows

1. Queue 020, the instrument the cut landed under.
2. Queue 025, the seven images that are what step 4 actually is, after the
   director folded the other 26 lines into the scene generator that has to
   exist anyway for the engine comparison.
3. The engine comparison itself. The timebox ends 2026-09-14 and does not
   extend.
