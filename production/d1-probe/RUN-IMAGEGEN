# The sentinel that runs .github/workflows/ledger-imagegen.yml.
#
# Touching this file and pushing generates Meridian signage on Jafar's PC, on
# the self-hosted runner labelled `ledger-pc` that four other workflows already
# use. It needs no click from him and no account: the model is Apache-2.0 and
# nothing here is purchased. The point of the lane is that this compute is
# FREE against the weekly Claude ceiling and runs while nobody is working.
#
# It exists because a workflow_dispatch workflow must live on the default
# branch to be dispatchable, and this one lives on the working branch by
# standing instruction. Delete this file and the push trigger together when
# the workflow reaches main; the dispatch entry there is already correct and
# takes limit / only / max_minutes as inputs.
#
# It sits HERE, beside DISPATCH, SETUP-MSVC, SETUP-BLENDER and FETCH-VIGNETTE,
# and NOT in ledger/Assets/StreamingAssets/Decals/generated, because that
# directory is the job's OUTPUT and a trigger inside an output directory is how
# a run re-fires itself.
#
# WHAT A SENTINEL PUSH GENERATES, and how to change it. THE SETTINGS ARE THE
# UNCOMMENTED LINES AT THE BOTTOM OF THIS FILE. The push path cannot carry
# dispatch inputs, so the size of a run has to be settable in the same commit
# that starts it; `imagegen.py --batch-settings` reads `limit:`, `only:` and
# `max_minutes:` from here and the workflow exports what it returns. Anything
# behind a `#` is prose, including this paragraph, and is never read as a
# setting. Precedence, printed in the log of every run: a dispatch input
# outranks this file, this file outranks the fallback (4 pictures, 60 min),
# and inside this file the LAST line for a key wins, because this file is
# appended to per run. `limit: all` means no cap on how many are generated;
# `--limit` bounds GENERATIONS rather than positions, so items already on disk
# that pass the skip check cost nothing either way.
#
# `max_minutes` IS CAPPED AT 250 by imagegen.py, and that is deliberate rather
# than mean. The generating loop must stop itself and hand back, because the
# step AFTER it is the one that commits: a job killed by its own timeout can
# leave the night's pictures on a PC nobody can reach. 250 < the generate
# step's 260 < the job's 300.
#
# WHAT THE FIRST RUN IS FOR. Proving the route, not filling the queue. 31 of
# the 45 written prompts have never been generated, and a night that produces
# 31 blank PNGs plus a manifest describing them as art is worse than a night
# with no run at all (leejet/stable-diffusion.cpp#1031: Z-Image on Vulkan can
# write a blank PNG and exit zero). So the first dispatch makes four, the run
# stops at the first blank, and the verdict re-measures every PNG from its own
# pixels before anything is called banked.
#
# One line per run, appended, so this file is also the dispatch log.
#
# run 1 - UNRUN as this file is written. Expected: sign_ferry,
# sign_harbour_master, sign_weighbridge and sign_telephone, the first four
# items of tools/imagegen/prompts.json with no PNG on disk, 768x512 each, an
# estimated 68 seconds each on the card measured 25 Aug. The verdict lands at
# ledger/Assets/StreamingAssets/Decals/generated/imagegen-verdict.txt with the
# commit on line 1.
#
# run 1 - RAN 2026-09-02 on b8b805f2 (run 33654488608), 105 s, FAILED at the
# setup step `The commit this run is measuring` in 0 s; all four work steps
# skipped; nothing banked, nothing carried forward. Cause: git ran in that
# step without the safe.directory env every other git step on ledger-pc
# carries, and the commit step's own git rev-parse succeeded under it in the
# same run. A second fault: the verdict was printed and staged=0. Ruling:
# game-design/decision-2026-09-02-imagegen-run1-stopper-and-run2.md.
#
# run 2 - fired by the push that lands that ruling. Same four items expected.
# The verdict's steps line now carries stopper= and shaFrom=, and the commit
# step prints every staging candidate with %q, so a trailing \r from Windows
# Python shows as $'\r' and the second fault is measured, not believed.

# run 3 - THE NIGHT RUN, and the first run under a verdict that measures the
# COMMIT rather than the output directory. Run 2 reported BANKED and
# `git show --name-only c685aa93` returned not one picture: it had regenerated
# four PNGs that were already on disk, byte for byte (all four sha256 in its
# manifest equal the ones cb332751 committed on 26 Aug, measured 2026-09-02),
# so `git add` staged nothing and the commit carried no art. BANKED now
# requires the pictures to be in what git is about to carry; a byte-identical
# regeneration reads NOTHING-NEW and a picture that is nowhere in the commit
# reads NOT-BANKED, loudly, with the file names.
# Expected tonight: 31 items that have never been generated, plus 14 already
# on disk which are now SKIPPED FOR FREE rather than remade (the resume record
# reads the recipe the previous manifest recorded; run 2 insisted on rebuilding
# it from a seed that SKIPPED rows do not carry, so it reconstructed zero of
# fourteen and spent six minutes of GPU reproducing four). About 64 minutes at
# the rate measured on 25 Aug, against a 250-minute cap.
# Also fixed: `attribution=failure` was this workflow's own scratch file. The
# sha step wrote `.imagegen-sha.err` into the workspace whether or not git
# failed, `.err` is in neither suffix list in tools/attribution-check.py, and
# the sweep reported 1 unclassified of 3919 walked. Reproduced in the
# container by touching that name; the file now lives in RUNNER_TEMP and the
# copy run 2 left on the runner is deleted at the start of the step.

run 4 - 2026-09-03 - THE FRAMING REGENERATION. Run 3 banked 31 pictures and
a QA pass then opened all 45 and found 41 of them are photographs of signs
standing in a street rather than plates that can go on a wall. One cause: the
sign, fascia, notice and poster families carried "photograph, straight-on flat
elevation" plus "deserted empty street". They now carry the orthographic
prefix the four wall plates already proved. 41 recipes moved, 4 did not.
EXPECT skipped=4 and remadeRecipeChanged=41; anything else means the recipe
arithmetic was wrong and the rest of this run's evidence is read in that light.

# ---------------------------------------------------------------------------
# THE SETTINGS. Uncommented lines only. Last-wins.
limit: all
max_minutes: 240

# ---------------------------------------------------------------------------
# run 5 - 2026-09-09 - THE HOOK COMPARISON, AND THE FIRST RUN THAT IS NOT THE
# SHIPPED LIBRARY. Jafar 2026-09-09 item 4(a): his Codex Hook concept sheet
# beside what this lane draws from the same description. Model against model,
# three passes on their side (creation prompt, edit pass, spatial correction,
# every one of them inspected and corrected by Codex itself) against two on
# ours (draw, inspect by vision, correct once). The spec is
# tools/imagegen/compare-hook-2026-09-09.json and NOT tools/imagegen/
# prompts.json, which is the whole reason nothing here can reach the shipped
# decals directory: 4 items, 688x1024, cfg 1.0, 8 steps, seeds 20260909 to
# 20260912 written into the spec before anything was generated. validate_spec
# on that file: 0 problems of 4 items examined, 0 forbidden-token hits and 0
# exclusion-scan hits over 229 words per item.
#
# WHAT IT COSTS, READ OFF THE TOOL'S OWN NUMBERS rather than typed here.
# estimate_seconds(688, 1024, cfg=1.0) = 152.4 s per image on the card
# COST_SOURCE names (RX 6700 / Vulkan / Z-Image-Turbo Q4_K / 8 steps), so
# 610 s = 10.2 min of GPU for all four, 2.5 min for the one that goes in the
# message. The measured neighbours are run 4's own manifest (sha 991aabf, 41
# images with seconds): 640x896 median 102 s over 19 images, 1024x512 median
# 91 s over 4, 768x512 median 56 s over 17. 688x1024 is 704512 px, 1.23x the
# largest of those areas, and no run here has banked that pair, so a blank is
# a live possibility and the run prints the blank verdict per image.
# max_minutes 30 is about three times the estimate and inside the ceiling of
# 250: the generating loop stops itself and hands back to the commit step.
#
# THE BLOCKER ABOVE IS CLEARED. This entry was written while
# .github/workflows/ledger-imagegen.yml read NEITHER spec nor out, which is
# why it said DO NOT PUSH. All four sites now carry the values and the
# workflow refuses rather than falling back: a sentinel that ASKS for a spec
# the workflow cannot pass stops the run before the probe and before the
# 6.7 GB, exit 7, with specPassed= and outPassed= printed.
#
# THE LINE NUMBERS THAT USED TO BE HERE ARE GONE ON PURPOSE. They were 437 to
# 446, 537, 578 and 556, and every one of them moved when the fix landed. A
# comment that pins a line number in another file is a claim that decays the
# next time somebody edits that file, and this one decayed within the hour.
#
# WHAT THE OLD STATE WOULD HAVE COST, kept because it is the reason this
# paragraph exists at all: a push would not have drawn the Hook. It would have
# read the 45-item library, refused because the four ids below are in no
# library item, and written a REFUSED manifest.json and ATTRIBUTION.json into
# ledger/Assets/StreamingAssets/Decals/generated, which the commit step stages
# BY NAME, over the provenance of 45 shipped pictures. Built is not running,
# rule 6, and its fallback was destructive.
#
# Every prose line in this entry begins with a #, and that is deliberate:
# batch_settings reads `key: value` off any line that is not commented out,
# and one uncommented sentence beginning with the word spec, out, only, limit
# or max_minutes would silently become the setting.
#
# THE SETTINGS. Uncommented lines only, last-wins, so these five override the
# run 4 pair above and nothing at all is inherited by accident.
spec: tools/imagegen/compare-hook-2026-09-09.json
out: production/art/compare/hook-2026-09-09
only: hook_compare,hook_compare_s2,hook_compare_s3,hook_compare_s4
limit: 4
max_minutes: 30

# ---------------------------------------------------------------------------
# run 6 - 2026-09-09 - PASS 2 OF THE HOOK COMPARISON, THE CORRECTION PASS.
# Run 5 banked all four pass-1 draws: imagegenVerdict=BANKED wroteThisRun=4
# blankThisRun=0 checkedThisRun=4, 154 to 157 s each, commit e4924cb7. THE
# FOUR WERE THEN OPENED AND INSPECTED BY VISION, which is the middle step of
# the method Jafar named on 2026-09-09 and the step Codex's own sheet had
# three of against our two.
#
# WHAT THE INSPECTION FOUND, and every one of these is a fault in OUR draw
# rather than a fault in the lane:
#   1. MICKEY'S is crisp and correctly spelled on seed 20260910 and garbled
#      on the other three. Every SECONDARY fascia is an illegible smear on
#      all four, where the reference sheet carries a legible HARBOUR FISH.
#   2. The three objects the pass-1 prompt already asked for, rope fender,
#      metal dustbin and beer firkin, were DROPPED BY THE MODEL on all four.
#      Only the four material swatches arrived. The prompt asked for both in
#      one sentence and the model rendered the first half.
#
# WHAT PASS 2 CHANGES, and it is one correction because the method allows one.
# The two fascia strings are named explicitly and every other shopfront is
# ordered to carry NO text at all, so the model draws two strings rather than
# a terrace of invented ones. The dropped objects are pulled onto their own
# separate named row rather than sharing a sentence with the swatches.
#
# WHAT IT DELIBERATELY DOES NOT DO, recorded as a judgement rather than left
# as an oversight: it does not ask for swatch LABEL text, which the reference
# sheet has. At 8 steps every additional string degrades the lettering that
# already works, so this CONCEDES A VISIBLE DIFFERENCE to protect the two
# fascias. Recommendation taken and logged per his standing order; he may
# reverse it and the next pass would carry the labels.
#
# THE SEED THAT LED IS THE ONE THAT WORKED. 20260910 is the pass-1 draw whose
# lettering came out right, and it leads here. A CHANGED PROMPT DOES NOT
# REPRODUCE A COMPOSITION AT A GIVEN SEED, so that is the closest available
# anchor and not a guarantee; three fresh seeds ride with it for spread.
#
# COST, off the tool's own estimate rather than typed here: the four pass-1
# draws measured 154.2, 154.5, 154.8 and 156.7 s at this exact size, so 620 s
# of GPU is a MEASURED neighbour rather than an estimate. max_minutes 30 is
# about three times that and inside the 250 ceiling.
#
# THE SPEC WAS BUILT WRONG FIRST AND THE ENTRY POINT CAUGHT IT. Written from
# scratch, it dropped content_rules, which is the clause forbidding real
# company names, real brand logos, trade marks and recognisable faces, and
# `plan --dry-run` refused with KeyError rather than drawing four pictures
# without it. Rebuilt by CARRYING EVERY TOP-LEVEL KEY off the pass-1 spec:
# 9 of 9 carried, 0 missing. Same fault class as a hand-written NEEDS tuple,
# and the same repair: derive the set, never hand-list it.
#
# THE SETTINGS. Uncommented lines only, last-wins, so these five override
# every pair above and nothing at all is inherited by accident.
spec: tools/imagegen/compare-hook-2026-09-09-pass2.json
out: production/art/compare/hook-2026-09-09-pass2
only: hook_pass2,hook_pass2_s2,hook_pass2_s3,hook_pass2_s4
limit: 4
max_minutes: 30

# ---------------------------------------------------------------------------
# run 7 - 2026-09-10 - PASS 2 AGAIN, AND THE REASON RUN 6 REFUSED IS FIXED.
# Run 6 read the pass-2 spec correctly (source=--spec, itemsInSpec=4) and then
# REFUSED TO START, banking nothing: imagegenVerdict=NO-RUN with
# why=nothing-generated-nothing-was-attempted.
#
# THE CAUSE, off the committed manifest and not off an argument. Four of four
# items: "the POSITIVE prompt still says ['no'] - an exclusion belongs in
# `negatives`, because a diffusion model reads the noun and draws it. That is
# what put a sign board on wall_soot_brick." THE CORRECTION PASS, WRITTEN TO
# FIX LETTERING, ASKED FOR "no text" TWICE IN THE HALF OF THE PROMPT THAT
# CANNOT EXPRESS "no". The guard was right and it cost nothing: it refused
# before a minute of GPU was spent.
#
# WHAT CHANGED. Both exclusions moved from the positive prompt into `negative`.
# "with no text on it at all" is now "plain unlettered painted timber", and
# "Both rows unlabelled, no text." is now "Both rows unlabelled.". The token is
# absent from 4 of 4 positive prompts and the exclusions are present in 4 of 4
# negatives, both counted rather than assumed.
#
# AND THE MESSAGE THAT SENT THE READER TO THE WRONG FILE IS FIXED. It
# hardcoded "prompts.json", so every --spec run refused while naming the one
# file such a run never touches. It now names no file rather than a wrong one.
# imagegen selftest after both changes: 144 passed, 0 failed.
#
# THE SETTINGS. Uncommented lines only, last-wins, so these five override every
# pair above and nothing at all is inherited by accident.
spec: tools/imagegen/compare-hook-2026-09-09-pass2.json
out: production/art/compare/hook-2026-09-09-pass2
only: hook_pass2,hook_pass2_s2,hook_pass2_s3,hook_pass2_s4
limit: 4
max_minutes: 30

# RUN 7: THE FAIRVIEW SHEET, AND THE FIRST CONCEPT THIS STUDIO AUTHORED.
# Jafar 2026-09-10: the local image lane is the concept route, the outside
# account is retired from production, and this sheet's prompt is authored from
# the form bible, the atlas, canon and the atlas-02 research with no use of the
# outside account's prompt files. Provenance table is in the spec's `source`
# block, 48 rows, and in production/art/concept-fairview-2026-09-10/DELIVERY.md.
#
# WHY FAIRVIEW. Two of the five atlas-02 research files are Fairview files, and
# the atlas's own Fairview row records that the existing outside concept covers
# the upper hill only. Thickest evidence, admitted gap.
#
# FOUR ITEMS, TWO ARMS, same concept and same sources: two draws of the authored
# prompt at 549 words, two of the same concept with the enumeration cut at 400
# words. The one adherence fact this lane has measured is that at 296 words the
# model dropped three requested objects on all four pass-1 draws, so this run
# also answers whether cutting the ask helps.
#
# VALIDATION BEFORE THE RUN, each number printed rather than asserted:
# validate_spec 0 problems of 4 items examined; positive-prompt exclusion words
# 0 of 542 and 0 of 387 words scanned; forbidden marks 0 of 45 tokens scanned;
# framing clause present 4 of 4; negative recorded and inert 4 of 4 at cfg 1.0.
#
# THE SETTINGS. Uncommented lines only, last-wins, so these five override every
# pair above and nothing at all is inherited by accident.
spec: production/art/concept-fairview-2026-09-10/fairview-sheet-2026-09-10.json
out: production/art/concept-fairview-2026-09-10/sheets
only: fairview_sheet,fairview_sheet_s2,fairview_sheet_short,fairview_sheet_short_s2
limit: 4
max_minutes: 30
