# The sentinel that runs .github/workflows/ledger-setup-msvc.yml.
#
# One-off machine setup: installs the Visual Studio 2022 BUILD TOOLS with the
# C++ workload on the build PC, which the UE probe found entirely absent.
# Idempotent: if MSVC is already there the workflow reports it and exits
# without touching anything, so touching this file again is free.
#
# Authorized by Jafar 2026-09-01.
#
# One line per run, appended, so the file is also the setup log.
run 1 - 2026-09-01 - install VS 2022 Build Tools, VCTools workload plus
recommended (which brings the Windows SDK UE also needs). Probe run 2 found
vswhere absent, so there is no Visual Studio of any kind on the machine.

run 2 - 2026-09-01 - the UE EDITOR needs more than the game does. Run 9's
cook reached UnrealBuildTool and died on SwarmInterface: "Could not find
NetFxSDK install dir; install a version of .NET Framework SDK at 4.6.0 or
higher." Adding Microsoft.Net.Component.4.8.SDK and the 4.6.2 targeting
pack to the existing Build Tools install. Same authorization as run 1 and
the same machine setup for the same purpose: no purchase, no account.
The guard that would have skipped this is fixed in the same commit; it
asked about one component and so could never add a second.
