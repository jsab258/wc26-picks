# Learning: the lessons pipeline (created 2026-08-31, Jafar's governance instruction)

This file holds ONLY the pipeline rules and the index of terminated lessons.
The lessons themselves live where they took effect: the amended rule, the
changed gate, the decision record. Agents inherit every lesson automatically
by reading the framework they already boot from; nobody re-reads a lessons
archive, because an archive nobody must read is an archive nobody reads.

## The pipeline
1. Sources: every production/queue/blocked/ item, every audit finding, every
   postmortem, every phase-exit retrospective finding.
2. Every source item MUST terminate in exactly one of:
   a. a rule edit in studio-v2/
   b. a gate change (a verify check, a lint, a pipeline station)
   c. a decision record in respec/decision-register/
   d. an explicit no-change note stating why nothing changes
3. The termination links back to the source, and the source links forward to
   the termination. An unterminated lesson from the past week FAILS the
   weekly process audit (production/queue/900).
4. The index below records each terminated lesson in one line with links.
   The index is also the HARVEST INPUT LIST.

## Harvest (mandatory phase-exit step; a phase cannot close without it)
1. Pull jsab258/game-studio as a sibling working directory.
2. From this index, take the lessons terminated since the last harvest and
   apply only those passing the PORTABILITY TEST: would this change apply
   unchanged to the next game? Portable: rules, gates, pipelines, the
   runner, the judge protocol, templates, waste lessons. Never portable:
   canon, era, cast, assets, tuned numbers.
3. REWRITE files rather than append, so game-studio always reads as a clean
   current framework, not a changelog.
4. Commit to game-studio main naming the phase and the source lessons.
   Summarize the diff in the morning brief.
5. Maintain the game-studio README status line: "framework distilled from
   LEDGER production, current through Phase N".
6. A harvest can also be enqueued manually at any time; the phase-exit one
   is the floor, not the ceiling.

## Index of terminated lessons
| id | date | source | terminated as | where |
|---|---|---|---|---|
| L1 | 2026-08-31 | D10 forbids the continuous-sync mechanism the template_sync gate WAS | gate change: template_sync retired in place | ledger/verify.py template_sync(); ledger-v2/respec/decision-register/D10-framework-freeze.md |
| L2 | 2026-08-31 | canon-gate refused the word "debts" (substring 'bt ' for a real brand) in a game about a book of uncollectable debts | gate change: brand matching word-bounded, both outcomes in the selftest | tools/canon-gate.py |
| L4 | 2026-08-31 | judge calibration sample 1 came back 48 PASS, 0 FAIL: a set with no rejecting case cannot separate a calibrated judge from one that always says PASS, and D7's zero-false-passes-on-canon clause was unmeasurable because the canon gate had already cleaned the bank | rule edit: a calibration sample MUST span the decision boundary and MUST contain canon violations by construction; sample 2 built to that shape | ledger-v2/studio-v2/verification.md; production/specs/judge-calibration-2-dialogue.md |
| L5 | 2026-08-31 | the judge's rejecting fixtures contain canon violations by construction, so the canon gate would refuse the fixture that makes the gate measurable | gate change: fixture path exempt by name, exemption printed when it bites, and a selftest proving the same text still refuses outside the fixture path | tools/canon-gate.py |
| L6 | 2026-09-01 | Jafar: "I don't want to be tested like I'm doing an exam at school, and from the beginning I said I want minimal manual work." A second grading sheet was built for him after sample 1 came back all PASS | rule edit: calibration and test set are separate artefacts with separate owners; the human grades only the positive boundary, the studio constructs and labels the negatives, and the ongoing audit is the check on that | ledger-v2/studio-v2/verification.md; production/specs/judge-test-set-1-dialogue.md |
| L7 | 2026-09-01 | six troubleshooting routes tried from reasoning produced a confident wrong diagnosis (Epic account entitlement); one search found a widely reported launcher bug and a thirty-second fix | explicit no-change note: no rule is added. "Search first" as a standing rule would be cargo cult, and the existing rule (suspect the instrument, check before asserting) already covers it. What is recorded is the discriminator: an account-state symptom and a UI-bug symptom are indistinguishable from the inside, so the cheap test is whether other people have seen it | production/queue/done/000-d1-ue5-install.md |
| L8 | 2026-09-01 | two different things were both called "the runner" (the CI build agent on the PC since 22 Aug, and the night loop written 31 Aug); I declared the night loop D1's critical path when the build agent had been doing machine-side work for ten days. Jafar caught it by asking what the .bat does | rule edit plus doc fix: tools/runner/README.md now opens by distinguishing them, and a task saying "the runner" without naming which is rejected at spec | tools/runner/README.md; production/d1-probe/plan.md |
| L9 | 2026-09-01 | the UE probe workflow's first run died on `pwsh: command not found`: I copied the Unity job's PATH bootstrap step and dropped its second half, the part that finds PowerShell 7 and the explicit exit code. The comment above the copy justified copying rather than referencing, and the copying was what was done badly | gate change plus a queued fix: the complete block is restored so the probe runs, and task 006 extracts it into one script both workflows call, dispatching both afterwards because the shared copy has to behave for the proven workflow too | .github/workflows/ledger-probe-unreal.yml; production/queue/006-dedup-runner-bootstrap.md |
| L10 | 2026-09-01 | the UE probe's run 2 measured correctly and shipped line 1 as `# UE machine probe -  @1788263890`, with the sha empty: the provenance line every verdict here carries, carrying none. A silent `git rev-parse` failure inside a pwsh step, hidden by ErrorActionPreference=Continue | gate change: read GITHUB_SHA, which the runner sets and which cannot be empty in a real run, with git as fallback and the literal SHA-UNKNOWN when both fail, because a blank where a sha belongs reads as a sha | .github/workflows/ledger-probe-unreal.yml |
| L3 | 2026-08-31 | tools/runner/README.md was written without checking the directory, which already housed the v1 build runner | no-change note: nothing was in fact overwritten (checked in git after the alarm); the README now names both tenants, and the standing rule "look before you write" already covers it | tools/runner/README.md |
| L11 | 2026-09-01 | the UE probe was built as a Program target. An installed (launcher) engine cannot build a Program target at all, so two build attempts and two round trips were spent on a shape that could never compile on the machine D1 is measuring | rule edit: a probe's TARGET SHAPE is part of its spec and must match the shipping shape of the thing being measured. A Program target was chosen because it was the cheapest thing to compile, which is a different quantity from the one worth pricing; the Game target that replaced it is both buildable and representative | ue-probe/Source/LedgerProbe.Target.cs; production/d1-probe/DISPATCH run 6 |
| L12 | 2026-09-01 | the first Unity build-time series pooled 188 runs under the label "on ledger-pc" when 116 of them were GitHub hosted runners. The number exists to sit beside a UE build measured on Jafar's PC, so pooling would have compared a rented CPU against his and called it an engine difference | gate change: the collector splits by runner class and reports two medians with two denominators, never one, and the selftest's accepting case is a mixed sample that must come back split | tools/d1-cycles.py; production/d1-probe/unity-build-steps.tsv |
| L13 | 2026-09-01 | docs-check printed "117/117 clean" while its root is game-design/ alone. Since the v2 respec landed there are 57 markdown documents in production/, ledger-v2/ and legacy/ that it has never opened, and a clean result read as a claim about all of them | gate change: the unwalked trees are named with their counts on every run. Widening the scope is deliberately NOT done here: the v2 package carries its own conventions and would go red on this one, so that is a decision rather than a tidy-up | tools/docs-check.py |
| L14 | 2026-09-01 | the UE probe's run 6 built cleanly and then exited 3 in six seconds having written nothing, so "the module never loaded" and "the module loaded and the test died" were indistinguishable. The step captured neither the game's streams nor UE's own log | gate change: the game's stdout and stderr are redirected and tailed, LedgerProbe.log is tailed by name, and the module writes a moduleStartup breadcrumb before doing any work so the two cases separate | .github/workflows/ledger-probe-unreal.yml; ue-probe/Source/LedgerProbe/Private/LedgerProbe.cpp |
| L15 | 2026-09-01 | run 6's coldBuildMinutes was honestly cold only because it was the first build ever to compile. Every later run inherits Intermediate/ and Binaries/ from the agent's persistent workspace, so the same key would have carried a warm number under a cold name | gate change: the step clears both directories before the cold build and prints clearedBeforeCold with its denominator, so the series stays comparable across runs | .github/workflows/ledger-probe-unreal.yml |
| L16 | 2026-09-01 | the first three live cycle rows were written from memory of which probe run was which, minutes after shipping a tool whose header says rows come from real edits and never from memory. Two of the three were wrong: the Program-to-Game edit was labelled onto the shared-environment cycle, and two cycles were merged into one 26-minute row | no-change note: the standing rule (never assert what you have not just checked) already covers it and a second rule would only decorate. What is recorded is the discriminator, because it is cheap and it worked: every endpoint here is a landed CI commit, so `git log -- <the evidence file>` prints the true ordering in one command, and the file now states that convention above its rows | production/d1-probe/cycles.tsv |
| L17 | 2026-09-01 | the MSVC setup workflow asked whether ONE component was present and reported ALREADY PRESENT if it was, so it could never add a second. When the UE editor turned out to need the .NET Framework SDK, the workflow whose entire job is machine setup would have skipped the setup and said everything was fine | gate change: a named required set checked component by component, only the missing ones installed, modify used against an existing install rather than a bare --add that quietly does nothing, and verifiedComponents printed with its denominator so all-three and one-of-three cannot read alike | .github/workflows/ledger-setup-msvc.yml |
| L18 | 2026-09-01 | two commits in one session carried the PREVIOUS commit's message, because the message was written to a fixed filename in a scratch directory that survives between sessions and then appended to. The append succeeded, the file was stale, the commit was clean, and nothing said so. Both were caught by eye. The commit feed is Jafar's only heartbeat between asks, so a commit describing work from an hour ago is a false report with a timestamp on it | gate change: a commit-msg hook refuses a subject identical to HEAD's, with an explicit typed escape for a deliberate amend. Narrow on purpose: it catches the whole-message-reused case exactly and cannot fire on an honest commit. Tested on both outcomes plus the escape before shipping | .githooks/commit-msg |
| L19 | 2026-09-01 | the canon gate refused the brand-bible spec for the word 'internet' in the sentence 'a town with no internet', which is a correct statement of canon rather than a violation. Same shape as L2 and as the three trademark false positives: the gate cannot tell a rule being stated from a rule being broken | no-change note: reworded, not loosened. Teaching a token screen to read negation would create a real hole ('no reason he would not use the internet' passes the same test), and canon.md is already exempt by name for exactly this reason with the exemption printed when it bites. What is recorded is that this WILL recur, because every spec states canon constraints, and the answer each time is to reword. If it starts costing real time, the fix is a narrow prefix exemption like the judge fixtures have, never a wider match | production/specs/brand-bible-v1.md; tools/canon-gate.py |
| L20 | 2026-09-01 | moving the probe module to PostConfigInit made the golden test work in the game and simultaneously broke the cook. A module loads in EVERY host that loads it, and one of those hosts is the cook commandlet's editor, so the cook started, loaded the module, ran the test and was told to quit before cooking anything. The failure read as "ensure that cooking was successful", which was true and gave no hint that the saboteur was the test | rule edit: a probe's test runs only when explicitly asked, on a switch the harness passes for exactly one invocation. IsRunningCommandlet would have worked today and would have been a guess about tomorrow. The general form: instrumentation that runs on load runs in every host, and a measurement that can end a process must never be reachable from a host that was not asked to be measured | ue-probe/Source/LedgerProbe/Private/LedgerProbe.cpp |
| L21 | 2026-09-01 | brand-verify's reader for canon.md's minted list was written twice as a regex and got the same real sentence wrong in opposite directions: stopping at the line end split "Meridian Ferry" into a brand called "Meridian", stopping at the last period swallowed the following prose and produced seven names. Both returned a plausible COUNT, which is what made each convincing | rule edit plus gate change: the parse is four explicit testable steps rather than a regex, and the live canon.md is itself a selftest fixture alongside the synthetic one. The transferable part is the tell: the right number of wrong names reads as success, so a parser's denominator is not evidence that it parsed the right thing | tools/brand-verify.py |
| L22 | 2026-09-01 | a staged Windows build ships TWO executables with the same name: a launcher stub at the archive root and the real binary under Binaries\\Win64. The newest-first pick took the stub, which starts the real one and returns, so it exited 1 in two seconds having written nothing, which reads exactly like the module failing to start | gate change: prefer the path under Binaries\\Win64 and print both counts rather than a total, so "two candidates, took the real one" and "two candidates, took a stub" cannot look alike. The transferable form: a newest-first pick is only safe when every candidate is the same KIND of thing, and two files with one name in one tree is the case where it is not | .github/workflows/ledger-probe-unreal.yml |
| L23 | 2026-09-01 | the quality ladder, whose enforcement clause in CLAUDE.md says "it is asked at close", lived only in game-design/queue.md, which the v2 respec superseded. From 31 Aug the rule had a live clause and no home in the tree anybody works in, and nothing announced the orphaning. Found by trying to obey the rule while closing the brand bible | rule edit plus doc fix: production/quality-ladder.md is the v2 home and the queue README's close step names it. The transferable finding is about respecs rather than ladders: superseding a document moves its CONTENT and silently drops any MECHANISM that pointed into it, so a respec's checklist needs a step that asks which live rules aimed at the retired tree | production/quality-ladder.md; production/queue/README.md |
| L24 | 2026-09-01 | I committed and pushed the step-2 shot code describing it as HELD from dispatch, then realised two jobs were already queued and that dispatch takes a BRANCH, so untested C++ might ride a run meant to prove something else. Checked rather than worried: both queued runs carry head_sha 4165bf5, the commit before the shot code, so it did not ride them | no-change note, with a measurement recorded and NOT generalised. n=2. Both runs pinned the dispatch-time sha, which does not overturn CLAUDE.md's measured claim that four dispatched shas were never built; that was a larger sample and may have involved CI moving the branch under itself. The operational rule is unchanged and is the one that is safe either way: watch by ancestry, never by run name or branch movement. What this entry is really for is the habit: the cost of checking was one API call and the cost of assuming was a wrong belief in either direction | production/d1-probe/DISPATCH |
